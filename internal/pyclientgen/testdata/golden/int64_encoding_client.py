# Code generated by protoc-gen-onekit-py-client. DO NOT EDIT.
# source: int64_encoding.proto

from __future__ import annotations

import base64
import binascii
import json
import time
import urllib.error
import urllib.parse
import urllib.request
from dataclasses import dataclass, field
from datetime import datetime, timezone
from enum import IntEnum
from typing import Any, AsyncIterator, Iterator, Mapping, Optional, Protocol, Sequence, Union

@dataclass
class HttpResponse:
    """Minimal HTTP response shape returned by every HttpTransport."""
    status: int
    headers: Mapping[str, str]
    body: bytes


class HttpTransport(Protocol):
    """Duck-typed HTTP transport. Implement this to plug in requests/httpx/aiohttp."""
    def request(
        self,
        method: str,
        url: str,
        headers: Mapping[str, str],
        body: Optional[bytes],
        timeout: Optional[float],
    ) -> HttpResponse: ...


class UrllibTransport:
    """Default transport built on the Python standard library."""
    def request(
        self,
        method: str,
        url: str,
        headers: Mapping[str, str],
        body: Optional[bytes],
        timeout: Optional[float],
    ) -> HttpResponse:
        req = urllib.request.Request(url=url, method=method, data=body)
        for key, value in headers.items():
            req.add_header(key, value)
        try:
            with urllib.request.urlopen(req, timeout=timeout) as resp:
                return HttpResponse(
                    status=resp.status,
                    headers={k: v for k, v in resp.headers.items()},
                    body=resp.read(),
                )
        except urllib.error.HTTPError as exc:
            return HttpResponse(
                status=exc.code,
                headers={k: v for k, v in exc.headers.items()} if exc.headers else {},
                body=exc.read() if hasattr(exc, "read") else b"",
            )


@dataclass
class FieldViolation:
    """Single validation violation, matching onekit.http.FieldViolation."""
    field: str
    description: str = ""


class ApiError(Exception):
    """Base exception for any non-2xx HTTP response."""
    def __init__(
        self,
        status: int,
        body: bytes,
        headers: Optional[Mapping[str, str]] = None,
    ) -> None:
        self.status = status
        self.body = body
        self.headers = headers or {}
        super().__init__(f"HTTP {status}")


class ValidationError(ApiError):
    """Raised on HTTP 400 when the server returns onekit.http.ValidationError JSON."""
    def __init__(
        self,
        status: int,
        body: bytes,
        headers: Optional[Mapping[str, str]] = None,
        violations: Optional[Sequence[FieldViolation]] = None,
    ) -> None:
        super().__init__(status, body, headers)
        self.violations: list[FieldViolation] = list(violations or [])


_ERROR_CLASSES: list[tuple[type[ApiError], set[str]]] = [
]


@dataclass
class GetInt64TestRequest:
    """Generated from proto message testdata.int64encoding.GetInt64TestRequest."""
    id: str = ""

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        d["id"] = self.id
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "GetInt64TestRequest":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "id" in data and data["id"] is not None:
            kwargs["id"] = str(data["id"])
        return cls(**kwargs)

@dataclass
class Int64EncodingTest:
    """Generated from proto message testdata.int64encoding.Int64EncodingTest."""
    default_int64: str = "0"
    string_int64: str = "0"
    number_int64: int = 0
    default_uint64: str = "0"
    number_uint64: int = 0
    number_sint64: int = 0
    number_sfixed64: int = 0
    number_fixed64: int = 0
    repeated_number_int64: list[int] = field(default_factory=list)
    repeated_default_int64: list[str] = field(default_factory=list)
    optional_number_int64: Optional[int] = None
    commented_number_int64: int = 0

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        d["defaultInt64"] = str(self.default_int64)
        d["stringInt64"] = str(self.string_int64)
        d["numberInt64"] = self.number_int64
        d["defaultUint64"] = str(self.default_uint64)
        d["numberUint64"] = self.number_uint64
        d["numberSint64"] = self.number_sint64
        d["numberSfixed64"] = self.number_sfixed64
        d["numberFixed64"] = self.number_fixed64
        if self.repeated_number_int64:
            d["repeatedNumberInt64"] = [v for v in self.repeated_number_int64]
        if self.repeated_default_int64:
            d["repeatedDefaultInt64"] = [str(v) for v in self.repeated_default_int64]
        if self.optional_number_int64 is not None:
            d["optionalNumberInt64"] = self.optional_number_int64
        d["commentedNumberInt64"] = self.commented_number_int64
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "Int64EncodingTest":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "defaultInt64" in data and data["defaultInt64"] is not None:
            kwargs["default_int64"] = str(data["defaultInt64"])
        if "stringInt64" in data and data["stringInt64"] is not None:
            kwargs["string_int64"] = str(data["stringInt64"])
        if "numberInt64" in data and data["numberInt64"] is not None:
            kwargs["number_int64"] = int(data["numberInt64"])
        if "defaultUint64" in data and data["defaultUint64"] is not None:
            kwargs["default_uint64"] = str(data["defaultUint64"])
        if "numberUint64" in data and data["numberUint64"] is not None:
            kwargs["number_uint64"] = int(data["numberUint64"])
        if "numberSint64" in data and data["numberSint64"] is not None:
            kwargs["number_sint64"] = int(data["numberSint64"])
        if "numberSfixed64" in data and data["numberSfixed64"] is not None:
            kwargs["number_sfixed64"] = int(data["numberSfixed64"])
        if "numberFixed64" in data and data["numberFixed64"] is not None:
            kwargs["number_fixed64"] = int(data["numberFixed64"])
        if "repeatedNumberInt64" in data and data["repeatedNumberInt64"] is not None:
            kwargs["repeated_number_int64"] = [int(v) for v in data["repeatedNumberInt64"]]
        if "repeatedDefaultInt64" in data and data["repeatedDefaultInt64"] is not None:
            kwargs["repeated_default_int64"] = [str(v) for v in data["repeatedDefaultInt64"]]
        if "optionalNumberInt64" in data and data["optionalNumberInt64"] is not None:
            kwargs["optional_number_int64"] = int(data["optionalNumberInt64"])
        if "commentedNumberInt64" in data and data["commentedNumberInt64"] is not None:
            kwargs["commented_number_int64"] = int(data["commentedNumberInt64"])
        return cls(**kwargs)

@dataclass
class Int64EncodingServiceClientOptions:
    """Construct-time options for Int64EncodingServiceClient."""
    transport: Optional[HttpTransport] = None
    default_headers: Optional[Mapping[str, str]] = None
    timeout: Optional[float] = None
    content_type: str = "application/json"
    # Total attempts including the first (1 = no retries). Transport errors and
    # HTTP 429/502/503/504 are retried with exponential backoff. SSE never retries.
    max_retry_attempts: int = 1
    # Delay in seconds before the first retry; doubles on each subsequent retry.
    retry_backoff: float = 0.25


@dataclass
class Int64EncodingServiceCallOptions:
    """Per-call options for Int64EncodingServiceClient methods."""
    headers: Optional[Mapping[str, str]] = None
    timeout: Optional[float] = None
    content_type: Optional[str] = None


class Int64EncodingServiceClient:
    """Generated client for testdata.int64encoding.Int64EncodingService."""
    def __init__(
        self,
        base_url: str,
        options: Optional[Int64EncodingServiceClientOptions] = None,
    ) -> None:
        self._base_url = base_url.rstrip("/")
        opts = options or Int64EncodingServiceClientOptions()
        self._transport: HttpTransport = opts.transport or UrllibTransport()
        self._default_headers: dict[str, str] = dict(opts.default_headers or {})
        self._timeout = opts.timeout
        self._content_type = opts.content_type
        self._max_retry_attempts = opts.max_retry_attempts
        self._retry_backoff = opts.retry_backoff

    def get_int64_test(
        self,
        req: GetInt64TestRequest,
        options: Optional[Int64EncodingServiceCallOptions] = None,
    ) -> Int64EncodingTest:
        """Calls testdata.int64encoding.Int64EncodingService.GetInt64Test."""
        opts = options or Int64EncodingServiceCallOptions()
        content_type = opts.content_type or self._content_type
        if content_type != "application/json":
            raise NotImplementedError("only application/json is implemented")
        path = "/api/v1/test/int64/{id}"
        path = path.replace("{id}", urllib.parse.quote(str(req.id), safe=""))
        headers: dict[str, str] = dict(self._default_headers)
        headers["Content-Type"] = content_type
        headers["Accept"] = "application/json"
        if opts.headers:
            headers.update(opts.headers)
        body: Optional[bytes] = None
        resp = self._request_with_retries(
            method="GET",
            url=self._base_url + path,
            headers=headers,
            body=body,
            timeout=opts.timeout if opts.timeout is not None else self._timeout,
        )
        if resp.status >= 400:
            self._raise_for_status(resp)
        if not resp.body:
            return Int64EncodingTest()
        return Int64EncodingTest.from_dict(json.loads(resp.body))

    def _request_with_retries(
        self,
        method: str,
        url: str,
        headers: Mapping[str, str],
        body: Optional[bytes],
        timeout: Optional[float],
    ) -> HttpResponse:
        """Executes a request, retrying transport errors and 429/502/503/504."""
        attempts = max(1, self._max_retry_attempts)
        last_exc: Optional[Exception] = None
        for attempt in range(attempts):
            if attempt > 0:
                time.sleep(self._retry_backoff * (2 ** (attempt - 1)))
            try:
                resp = self._transport.request(
                    method=method, url=url, headers=headers, body=body, timeout=timeout,
                )
            except Exception as exc:  # noqa: BLE001 - transport errors vary by implementation
                last_exc = exc
                continue
            if attempt < attempts - 1 and resp.status in (429, 502, 503, 504):
                continue
            return resp
        assert last_exc is not None
        raise last_exc

    def _raise_for_status(self, resp: HttpResponse) -> None:
        """Map a non-2xx response to the most specific exception available."""
        body = resp.body or b""
        parsed: Any = None
        ctype = (resp.headers or {}).get("Content-Type", "")
        looks_jsonish = "json" in ctype.lower() or body[:1] in (b"{", b"[")
        if looks_jsonish:
            try:
                parsed = json.loads(body.decode("utf-8"))
            except (ValueError, UnicodeDecodeError):
                parsed = None
        if resp.status == 400 and isinstance(parsed, dict) and "violations" in parsed:
            violations = [
                FieldViolation(field=v.get("field", ""), description=v.get("description", ""))
                for v in parsed.get("violations", [])
            ]
            raise ValidationError(resp.status, body, resp.headers, violations)
        if isinstance(parsed, dict):
            for err_cls, required_keys in _ERROR_CLASSES:
                if required_keys and required_keys.issubset(parsed.keys()):
                    raise err_cls.populate(resp.status, body, resp.headers, parsed)
        raise ApiError(resp.status, body, resp.headers)

