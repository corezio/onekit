# Code generated by protoc-gen-onekit-py-client. DO NOT EDIT.
# source: unwrap.proto

from __future__ import annotations

import base64
import binascii
import json
import time
import urllib.error
import urllib.parse
import urllib.request
from dataclasses import dataclass, field
from datetime import datetime, timezone
from enum import IntEnum
from typing import Any, AsyncIterator, Iterator, Mapping, Optional, Protocol, Sequence, Union

@dataclass
class HttpResponse:
    """Minimal HTTP response shape returned by every HttpTransport."""
    status: int
    headers: Mapping[str, str]
    body: bytes


class HttpTransport(Protocol):
    """Duck-typed HTTP transport. Implement this to plug in requests/httpx/aiohttp."""
    def request(
        self,
        method: str,
        url: str,
        headers: Mapping[str, str],
        body: Optional[bytes],
        timeout: Optional[float],
    ) -> HttpResponse: ...


class UrllibTransport:
    """Default transport built on the Python standard library."""
    def request(
        self,
        method: str,
        url: str,
        headers: Mapping[str, str],
        body: Optional[bytes],
        timeout: Optional[float],
    ) -> HttpResponse:
        req = urllib.request.Request(url=url, method=method, data=body)
        for key, value in headers.items():
            req.add_header(key, value)
        try:
            with urllib.request.urlopen(req, timeout=timeout) as resp:
                return HttpResponse(
                    status=resp.status,
                    headers={k: v for k, v in resp.headers.items()},
                    body=resp.read(),
                )
        except urllib.error.HTTPError as exc:
            return HttpResponse(
                status=exc.code,
                headers={k: v for k, v in exc.headers.items()} if exc.headers else {},
                body=exc.read() if hasattr(exc, "read") else b"",
            )


@dataclass
class FieldViolation:
    """Single validation violation, matching onekit.http.FieldViolation."""
    field: str
    description: str = ""


class ApiError(Exception):
    """Base exception for any non-2xx HTTP response."""
    def __init__(
        self,
        status: int,
        body: bytes,
        headers: Optional[Mapping[str, str]] = None,
    ) -> None:
        self.status = status
        self.body = body
        self.headers = headers or {}
        super().__init__(f"HTTP {status}")


class ValidationError(ApiError):
    """Raised on HTTP 400 when the server returns onekit.http.ValidationError JSON."""
    def __init__(
        self,
        status: int,
        body: bytes,
        headers: Optional[Mapping[str, str]] = None,
        violations: Optional[Sequence[FieldViolation]] = None,
    ) -> None:
        super().__init__(status, body, headers)
        self.violations: list[FieldViolation] = list(violations or [])


_ERROR_CLASSES: list[tuple[type[ApiError], set[str]]] = [
]


@dataclass
class GetOptionBarsRequest:
    """Generated from proto message test.httpgen.unwrap.GetOptionBarsRequest."""
    symbols: list[str] = field(default_factory=list)
    start_date: str = ""
    end_date: str = ""

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        if self.symbols:
            d["symbols"] = [v for v in self.symbols]
        d["startDate"] = self.start_date
        d["endDate"] = self.end_date
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "GetOptionBarsRequest":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "symbols" in data and data["symbols"] is not None:
            kwargs["symbols"] = [str(v) for v in data["symbols"]]
        if "startDate" in data and data["startDate"] is not None:
            kwargs["start_date"] = str(data["startDate"])
        if "endDate" in data and data["endDate"] is not None:
            kwargs["end_date"] = str(data["endDate"])
        return cls(**kwargs)

@dataclass
class GetOptionBarsResponse:
    """Generated from proto message test.httpgen.unwrap.GetOptionBarsResponse."""
    bars: dict[str, OptionBarsList] = field(default_factory=dict)
    next_page_token: str = ""

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        if self.bars:
            d["bars"] = {k: [x.to_dict() for x in v.bars] for k, v in self.bars.items()}
        d["nextPageToken"] = self.next_page_token
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "GetOptionBarsResponse":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "bars" in data and data["bars"] is not None:
            kwargs["bars"] = {
                k: OptionBarsList(bars=[OptionBar.from_dict(x) for x in v])
                for k, v in data["bars"].items()
            }
        if "nextPageToken" in data and data["nextPageToken"] is not None:
            kwargs["next_page_token"] = str(data["nextPageToken"])
        return cls(**kwargs)

@dataclass
class IntList:
    """Generated from proto message test.httpgen.unwrap.IntList."""
    values: list[int] = field(default_factory=list)

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready value (root-unwrapped)."""
        return [v for v in self.values]

    @classmethod
    def from_dict(cls, data: Any) -> "IntList":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        return cls(values=[int(v) for v in data])

@dataclass
class MixedResponse:
    """Generated from proto message test.httpgen.unwrap.MixedResponse."""
    unwrapped_bars: dict[str, OptionBarsList] = field(default_factory=dict)
    regular_bars: dict[str, RegularWrapper] = field(default_factory=dict)
    status: str = ""

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        if self.unwrapped_bars:
            d["unwrappedBars"] = {k: [x.to_dict() for x in v.bars] for k, v in self.unwrapped_bars.items()}
        if self.regular_bars:
            d["regularBars"] = {k: v.to_dict() for k, v in self.regular_bars.items()}
        d["status"] = self.status
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "MixedResponse":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "unwrappedBars" in data and data["unwrappedBars"] is not None:
            kwargs["unwrapped_bars"] = {
                k: OptionBarsList(bars=[OptionBar.from_dict(x) for x in v])
                for k, v in data["unwrappedBars"].items()
            }
        if "regularBars" in data and data["regularBars"] is not None:
            kwargs["regular_bars"] = {k: RegularWrapper.from_dict(v) for k, v in data["regularBars"].items()}
        if "status" in data and data["status"] is not None:
            kwargs["status"] = str(data["status"])
        return cls(**kwargs)

@dataclass
class OptionBar:
    """Generated from proto message test.httpgen.unwrap.OptionBar."""
    symbol: str = ""
    price: float = 0.0
    volume: str = "0"
    timestamp: str = ""

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        d["symbol"] = self.symbol
        d["price"] = self.price
        d["volume"] = str(self.volume)
        d["timestamp"] = self.timestamp
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "OptionBar":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "symbol" in data and data["symbol"] is not None:
            kwargs["symbol"] = str(data["symbol"])
        if "price" in data and data["price"] is not None:
            kwargs["price"] = float(data["price"])
        if "volume" in data and data["volume"] is not None:
            kwargs["volume"] = str(data["volume"])
        if "timestamp" in data and data["timestamp"] is not None:
            kwargs["timestamp"] = str(data["timestamp"])
        return cls(**kwargs)

@dataclass
class OptionBarsList:
    """Generated from proto message test.httpgen.unwrap.OptionBarsList."""
    bars: list[OptionBar] = field(default_factory=list)

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready value (root-unwrapped)."""
        return [v.to_dict() for v in self.bars]

    @classmethod
    def from_dict(cls, data: Any) -> "OptionBarsList":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        return cls(bars=[OptionBar.from_dict(v) for v in data])

@dataclass
class RegularWrapper:
    """Generated from proto message test.httpgen.unwrap.RegularWrapper."""
    items: list[OptionBar] = field(default_factory=list)

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        if self.items:
            d["items"] = [v.to_dict() for v in self.items]
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "RegularWrapper":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "items" in data and data["items"] is not None:
            kwargs["items"] = [OptionBar.from_dict(v) for v in data["items"]]
        return cls(**kwargs)

@dataclass
class RootMapResponse:
    """Generated from proto message test.httpgen.unwrap.RootMapResponse."""
    people: dict[str, OptionBar] = field(default_factory=dict)

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready value (root-unwrapped)."""
        return {k: v.to_dict() for k, v in self.people.items()}

    @classmethod
    def from_dict(cls, data: Any) -> "RootMapResponse":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        return cls(people={k: OptionBar.from_dict(v) for k, v in data.items()})

@dataclass
class RootMapScalarListResponse:
    """Generated from proto message test.httpgen.unwrap.RootMapScalarListResponse."""
    groups: dict[str, IntList] = field(default_factory=dict)

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready value (root-unwrapped)."""
        return {k: v.to_dict() for k, v in self.groups.items()}

    @classmethod
    def from_dict(cls, data: Any) -> "RootMapScalarListResponse":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        return cls(groups={k: IntList.from_dict(v) for k, v in data.items()})

@dataclass
class RootMapWithValueUnwrapResponse:
    """Generated from proto message test.httpgen.unwrap.RootMapWithValueUnwrapResponse."""
    data: dict[str, OptionBarsList] = field(default_factory=dict)

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready value (root-unwrapped)."""
        return {k: v.to_dict() for k, v in self.data.items()}

    @classmethod
    def from_dict(cls, data: Any) -> "RootMapWithValueUnwrapResponse":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        return cls(data={k: OptionBarsList.from_dict(v) for k, v in data.items()})

@dataclass
class RootRepeatedResponse:
    """Generated from proto message test.httpgen.unwrap.RootRepeatedResponse."""
    items: list[OptionBar] = field(default_factory=list)

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready value (root-unwrapped)."""
        return [v.to_dict() for v in self.items]

    @classmethod
    def from_dict(cls, data: Any) -> "RootRepeatedResponse":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        return cls(items=[OptionBar.from_dict(v) for v in data])

@dataclass
class ScalarMapResponse:
    """Generated from proto message test.httpgen.unwrap.ScalarMapResponse."""
    data: dict[str, IntList] = field(default_factory=dict)

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        if self.data:
            d["data"] = {k: [x for x in v.values] for k, v in self.data.items()}
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "ScalarMapResponse":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "data" in data and data["data"] is not None:
            kwargs["data"] = {
                k: IntList(values=[int(x) for x in v])
                for k, v in data["data"].items()
            }
        return cls(**kwargs)

@dataclass
class ScalarRootMapResponse:
    """Generated from proto message test.httpgen.unwrap.ScalarRootMapResponse."""
    counts: dict[str, int] = field(default_factory=dict)

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready value (root-unwrapped)."""
        return {k: v for k, v in self.counts.items()}

    @classmethod
    def from_dict(cls, data: Any) -> "ScalarRootMapResponse":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        return cls(counts={k: int(v) for k, v in data.items()})

@dataclass
class ScalarRootRepeatedResponse:
    """Generated from proto message test.httpgen.unwrap.ScalarRootRepeatedResponse."""
    names: list[str] = field(default_factory=list)

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready value (root-unwrapped)."""
        return [v for v in self.names]

    @classmethod
    def from_dict(cls, data: Any) -> "ScalarRootRepeatedResponse":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        return cls(names=[str(v) for v in data])

@dataclass
class OptionDataServiceClientOptions:
    """Construct-time options for OptionDataServiceClient."""
    transport: Optional[HttpTransport] = None
    default_headers: Optional[Mapping[str, str]] = None
    timeout: Optional[float] = None
    content_type: str = "application/json"
    # Total attempts including the first (1 = no retries). Transport errors and
    # HTTP 429/502/503/504 are retried with exponential backoff. SSE never retries.
    max_retry_attempts: int = 1
    # Delay in seconds before the first retry; doubles on each subsequent retry.
    retry_backoff: float = 0.25


@dataclass
class OptionDataServiceCallOptions:
    """Per-call options for OptionDataServiceClient methods."""
    headers: Optional[Mapping[str, str]] = None
    timeout: Optional[float] = None
    content_type: Optional[str] = None


class OptionDataServiceClient:
    """Generated client for test.httpgen.unwrap.OptionDataService."""
    def __init__(
        self,
        base_url: str,
        options: Optional[OptionDataServiceClientOptions] = None,
    ) -> None:
        self._base_url = base_url.rstrip("/")
        opts = options or OptionDataServiceClientOptions()
        self._transport: HttpTransport = opts.transport or UrllibTransport()
        self._default_headers: dict[str, str] = dict(opts.default_headers or {})
        self._timeout = opts.timeout
        self._content_type = opts.content_type
        self._max_retry_attempts = opts.max_retry_attempts
        self._retry_backoff = opts.retry_backoff

    def get_option_bars(
        self,
        req: GetOptionBarsRequest,
        options: Optional[OptionDataServiceCallOptions] = None,
    ) -> GetOptionBarsResponse:
        """Calls test.httpgen.unwrap.OptionDataService.GetOptionBars."""
        opts = options or OptionDataServiceCallOptions()
        content_type = opts.content_type or self._content_type
        if content_type != "application/json":
            raise NotImplementedError("only application/json is implemented")
        path = "/api/v1/options/bars"
        headers: dict[str, str] = dict(self._default_headers)
        headers["Content-Type"] = content_type
        headers["Accept"] = "application/json"
        if opts.headers:
            headers.update(opts.headers)
        body = json.dumps(req.to_dict()).encode("utf-8")
        resp = self._request_with_retries(
            method="POST",
            url=self._base_url + path,
            headers=headers,
            body=body,
            timeout=opts.timeout if opts.timeout is not None else self._timeout,
        )
        if resp.status >= 400:
            self._raise_for_status(resp)
        if not resp.body:
            return GetOptionBarsResponse()
        return GetOptionBarsResponse.from_dict(json.loads(resp.body))

    def _request_with_retries(
        self,
        method: str,
        url: str,
        headers: Mapping[str, str],
        body: Optional[bytes],
        timeout: Optional[float],
    ) -> HttpResponse:
        """Executes a request, retrying transport errors and 429/502/503/504."""
        attempts = max(1, self._max_retry_attempts)
        last_exc: Optional[Exception] = None
        for attempt in range(attempts):
            if attempt > 0:
                time.sleep(self._retry_backoff * (2 ** (attempt - 1)))
            try:
                resp = self._transport.request(
                    method=method, url=url, headers=headers, body=body, timeout=timeout,
                )
            except Exception as exc:  # noqa: BLE001 - transport errors vary by implementation
                last_exc = exc
                continue
            if attempt < attempts - 1 and resp.status in (429, 502, 503, 504):
                continue
            return resp
        assert last_exc is not None
        raise last_exc

    def _raise_for_status(self, resp: HttpResponse) -> None:
        """Map a non-2xx response to the most specific exception available."""
        body = resp.body or b""
        parsed: Any = None
        ctype = (resp.headers or {}).get("Content-Type", "")
        looks_jsonish = "json" in ctype.lower() or body[:1] in (b"{", b"[")
        if looks_jsonish:
            try:
                parsed = json.loads(body.decode("utf-8"))
            except (ValueError, UnicodeDecodeError):
                parsed = None
        if resp.status == 400 and isinstance(parsed, dict) and "violations" in parsed:
            violations = [
                FieldViolation(field=v.get("field", ""), description=v.get("description", ""))
                for v in parsed.get("violations", [])
            ]
            raise ValidationError(resp.status, body, resp.headers, violations)
        if isinstance(parsed, dict):
            for err_cls, required_keys in _ERROR_CLASSES:
                if required_keys and required_keys.issubset(parsed.keys()):
                    raise err_cls.populate(resp.status, body, resp.headers, parsed)
        raise ApiError(resp.status, body, resp.headers)

@dataclass
class UnwrapServiceClientOptions:
    """Construct-time options for UnwrapServiceClient."""
    transport: Optional[HttpTransport] = None
    default_headers: Optional[Mapping[str, str]] = None
    timeout: Optional[float] = None
    content_type: str = "application/json"
    # Total attempts including the first (1 = no retries). Transport errors and
    # HTTP 429/502/503/504 are retried with exponential backoff. SSE never retries.
    max_retry_attempts: int = 1
    # Delay in seconds before the first retry; doubles on each subsequent retry.
    retry_backoff: float = 0.25


@dataclass
class UnwrapServiceCallOptions:
    """Per-call options for UnwrapServiceClient methods."""
    headers: Optional[Mapping[str, str]] = None
    timeout: Optional[float] = None
    content_type: Optional[str] = None


class UnwrapServiceClient:
    """Generated client for test.httpgen.unwrap.UnwrapService."""
    def __init__(
        self,
        base_url: str,
        options: Optional[UnwrapServiceClientOptions] = None,
    ) -> None:
        self._base_url = base_url.rstrip("/")
        opts = options or UnwrapServiceClientOptions()
        self._transport: HttpTransport = opts.transport or UrllibTransport()
        self._default_headers: dict[str, str] = dict(opts.default_headers or {})
        self._timeout = opts.timeout
        self._content_type = opts.content_type
        self._max_retry_attempts = opts.max_retry_attempts
        self._retry_backoff = opts.retry_backoff

    def get_option_bars(
        self,
        req: GetOptionBarsRequest,
        options: Optional[UnwrapServiceCallOptions] = None,
    ) -> GetOptionBarsResponse:
        """Calls test.httpgen.unwrap.UnwrapService.GetOptionBars."""
        opts = options or UnwrapServiceCallOptions()
        content_type = opts.content_type or self._content_type
        if content_type != "application/json":
            raise NotImplementedError("only application/json is implemented")
        path = "/api/v1/options/bars"
        headers: dict[str, str] = dict(self._default_headers)
        headers["Content-Type"] = content_type
        headers["Accept"] = "application/json"
        if opts.headers:
            headers.update(opts.headers)
        body = json.dumps(req.to_dict()).encode("utf-8")
        resp = self._request_with_retries(
            method="POST",
            url=self._base_url + path,
            headers=headers,
            body=body,
            timeout=opts.timeout if opts.timeout is not None else self._timeout,
        )
        if resp.status >= 400:
            self._raise_for_status(resp)
        if not resp.body:
            return GetOptionBarsResponse()
        return GetOptionBarsResponse.from_dict(json.loads(resp.body))

    def get_root_map(
        self,
        req: GetOptionBarsRequest,
        options: Optional[UnwrapServiceCallOptions] = None,
    ) -> RootMapResponse:
        """Calls test.httpgen.unwrap.UnwrapService.GetRootMap."""
        opts = options or UnwrapServiceCallOptions()
        content_type = opts.content_type or self._content_type
        if content_type != "application/json":
            raise NotImplementedError("only application/json is implemented")
        path = "/api/v1/root/map"
        headers: dict[str, str] = dict(self._default_headers)
        headers["Content-Type"] = content_type
        headers["Accept"] = "application/json"
        if opts.headers:
            headers.update(opts.headers)
        body = json.dumps(req.to_dict()).encode("utf-8")
        resp = self._request_with_retries(
            method="POST",
            url=self._base_url + path,
            headers=headers,
            body=body,
            timeout=opts.timeout if opts.timeout is not None else self._timeout,
        )
        if resp.status >= 400:
            self._raise_for_status(resp)
        if not resp.body:
            return RootMapResponse()
        return RootMapResponse.from_dict(json.loads(resp.body))

    def get_root_repeated(
        self,
        req: GetOptionBarsRequest,
        options: Optional[UnwrapServiceCallOptions] = None,
    ) -> RootRepeatedResponse:
        """Calls test.httpgen.unwrap.UnwrapService.GetRootRepeated."""
        opts = options or UnwrapServiceCallOptions()
        content_type = opts.content_type or self._content_type
        if content_type != "application/json":
            raise NotImplementedError("only application/json is implemented")
        path = "/api/v1/root/repeated"
        headers: dict[str, str] = dict(self._default_headers)
        headers["Content-Type"] = content_type
        headers["Accept"] = "application/json"
        if opts.headers:
            headers.update(opts.headers)
        body = json.dumps(req.to_dict()).encode("utf-8")
        resp = self._request_with_retries(
            method="POST",
            url=self._base_url + path,
            headers=headers,
            body=body,
            timeout=opts.timeout if opts.timeout is not None else self._timeout,
        )
        if resp.status >= 400:
            self._raise_for_status(resp)
        if not resp.body:
            return RootRepeatedResponse()
        return RootRepeatedResponse.from_dict(json.loads(resp.body))

    def get_root_map_with_value_unwrap(
        self,
        req: GetOptionBarsRequest,
        options: Optional[UnwrapServiceCallOptions] = None,
    ) -> RootMapWithValueUnwrapResponse:
        """Calls test.httpgen.unwrap.UnwrapService.GetRootMapWithValueUnwrap."""
        opts = options or UnwrapServiceCallOptions()
        content_type = opts.content_type or self._content_type
        if content_type != "application/json":
            raise NotImplementedError("only application/json is implemented")
        path = "/api/v1/root/map-value-unwrap"
        headers: dict[str, str] = dict(self._default_headers)
        headers["Content-Type"] = content_type
        headers["Accept"] = "application/json"
        if opts.headers:
            headers.update(opts.headers)
        body = json.dumps(req.to_dict()).encode("utf-8")
        resp = self._request_with_retries(
            method="POST",
            url=self._base_url + path,
            headers=headers,
            body=body,
            timeout=opts.timeout if opts.timeout is not None else self._timeout,
        )
        if resp.status >= 400:
            self._raise_for_status(resp)
        if not resp.body:
            return RootMapWithValueUnwrapResponse()
        return RootMapWithValueUnwrapResponse.from_dict(json.loads(resp.body))

    def _request_with_retries(
        self,
        method: str,
        url: str,
        headers: Mapping[str, str],
        body: Optional[bytes],
        timeout: Optional[float],
    ) -> HttpResponse:
        """Executes a request, retrying transport errors and 429/502/503/504."""
        attempts = max(1, self._max_retry_attempts)
        last_exc: Optional[Exception] = None
        for attempt in range(attempts):
            if attempt > 0:
                time.sleep(self._retry_backoff * (2 ** (attempt - 1)))
            try:
                resp = self._transport.request(
                    method=method, url=url, headers=headers, body=body, timeout=timeout,
                )
            except Exception as exc:  # noqa: BLE001 - transport errors vary by implementation
                last_exc = exc
                continue
            if attempt < attempts - 1 and resp.status in (429, 502, 503, 504):
                continue
            return resp
        assert last_exc is not None
        raise last_exc

    def _raise_for_status(self, resp: HttpResponse) -> None:
        """Map a non-2xx response to the most specific exception available."""
        body = resp.body or b""
        parsed: Any = None
        ctype = (resp.headers or {}).get("Content-Type", "")
        looks_jsonish = "json" in ctype.lower() or body[:1] in (b"{", b"[")
        if looks_jsonish:
            try:
                parsed = json.loads(body.decode("utf-8"))
            except (ValueError, UnicodeDecodeError):
                parsed = None
        if resp.status == 400 and isinstance(parsed, dict) and "violations" in parsed:
            violations = [
                FieldViolation(field=v.get("field", ""), description=v.get("description", ""))
                for v in parsed.get("violations", [])
            ]
            raise ValidationError(resp.status, body, resp.headers, violations)
        if isinstance(parsed, dict):
            for err_cls, required_keys in _ERROR_CLASSES:
                if required_keys and required_keys.issubset(parsed.keys()):
                    raise err_cls.populate(resp.status, body, resp.headers, parsed)
        raise ApiError(resp.status, body, resp.headers)

