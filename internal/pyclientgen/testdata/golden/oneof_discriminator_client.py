# Code generated by protoc-gen-onekit-py-client. DO NOT EDIT.
# source: oneof_discriminator.proto

from __future__ import annotations

import base64
import binascii
import json
import time
import urllib.error
import urllib.parse
import urllib.request
from dataclasses import dataclass, field
from datetime import datetime, timezone
from enum import IntEnum
from typing import Any, AsyncIterator, Iterator, Mapping, Optional, Protocol, Sequence, Union

@dataclass
class HttpResponse:
    """Minimal HTTP response shape returned by every HttpTransport."""
    status: int
    headers: Mapping[str, str]
    body: bytes


class HttpTransport(Protocol):
    """Duck-typed HTTP transport. Implement this to plug in requests/httpx/aiohttp."""
    def request(
        self,
        method: str,
        url: str,
        headers: Mapping[str, str],
        body: Optional[bytes],
        timeout: Optional[float],
    ) -> HttpResponse: ...


class UrllibTransport:
    """Default transport built on the Python standard library."""
    def request(
        self,
        method: str,
        url: str,
        headers: Mapping[str, str],
        body: Optional[bytes],
        timeout: Optional[float],
    ) -> HttpResponse:
        req = urllib.request.Request(url=url, method=method, data=body)
        for key, value in headers.items():
            req.add_header(key, value)
        try:
            with urllib.request.urlopen(req, timeout=timeout) as resp:
                return HttpResponse(
                    status=resp.status,
                    headers={k: v for k, v in resp.headers.items()},
                    body=resp.read(),
                )
        except urllib.error.HTTPError as exc:
            return HttpResponse(
                status=exc.code,
                headers={k: v for k, v in exc.headers.items()} if exc.headers else {},
                body=exc.read() if hasattr(exc, "read") else b"",
            )


@dataclass
class FieldViolation:
    """Single validation violation, matching onekit.http.FieldViolation."""
    field: str
    description: str = ""


class ApiError(Exception):
    """Base exception for any non-2xx HTTP response."""
    def __init__(
        self,
        status: int,
        body: bytes,
        headers: Optional[Mapping[str, str]] = None,
    ) -> None:
        self.status = status
        self.body = body
        self.headers = headers or {}
        super().__init__(f"HTTP {status}")


class ValidationError(ApiError):
    """Raised on HTTP 400 when the server returns onekit.http.ValidationError JSON."""
    def __init__(
        self,
        status: int,
        body: bytes,
        headers: Optional[Mapping[str, str]] = None,
        violations: Optional[Sequence[FieldViolation]] = None,
    ) -> None:
        super().__init__(status, body, headers)
        self.violations: list[FieldViolation] = list(violations or [])


_ERROR_CLASSES: list[tuple[type[ApiError], set[str]]] = [
]


@dataclass
class FlattenedEvent:
    """Generated from proto message testdata.oneof_discriminator.FlattenedEvent."""
    id: str = ""
    text: Optional[TextContent] = None
    image: Optional[ImageContent] = None

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        d["id"] = self.id
        if self.text is not None:
            d["type"] = "text"
            for _k, _v in self.text.to_dict().items():
                d[_k] = _v
        if self.image is not None:
            d["type"] = "img"
            for _k, _v in self.image.to_dict().items():
                d[_k] = _v
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "FlattenedEvent":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "id" in data and data["id"] is not None:
            kwargs["id"] = str(data["id"])
        _disc = data.get("type")
        if _disc == "text":
            kwargs["text"] = TextContent.from_dict(data)
        if _disc == "img":
            kwargs["image"] = ImageContent.from_dict(data)
        return cls(**kwargs)

@dataclass
class ImageContent:
    """Generated from proto message testdata.oneof_discriminator.ImageContent."""
    url: str = ""
    width: int = 0
    height: int = 0

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        d["url"] = self.url
        d["width"] = self.width
        d["height"] = self.height
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "ImageContent":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "url" in data and data["url"] is not None:
            kwargs["url"] = str(data["url"])
        if "width" in data and data["width"] is not None:
            kwargs["width"] = int(data["width"])
        if "height" in data and data["height"] is not None:
            kwargs["height"] = int(data["height"])
        return cls(**kwargs)

@dataclass
class NestedEvent:
    """Generated from proto message testdata.oneof_discriminator.NestedEvent."""
    id: str = ""
    text: Optional[TextContent] = None
    image: Optional[ImageContent] = None
    video: Optional[VideoContent] = None

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        d["id"] = self.id
        if self.text is not None:
            d["kind"] = "text"
            d["text"] = self.text.to_dict()
        if self.image is not None:
            d["kind"] = "image"
            d["image"] = self.image.to_dict()
        if self.video is not None:
            d["kind"] = "vid"
            d["video"] = self.video.to_dict()
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "NestedEvent":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "id" in data and data["id"] is not None:
            kwargs["id"] = str(data["id"])
        _disc = data.get("kind")
        if _disc == "text":
            if "text" in data:
                kwargs["text"] = TextContent.from_dict(data["text"])
        if _disc == "image":
            if "image" in data:
                kwargs["image"] = ImageContent.from_dict(data["image"])
        if _disc == "vid":
            if "video" in data:
                kwargs["video"] = VideoContent.from_dict(data["video"])
        return cls(**kwargs)

@dataclass
class PlainEvent:
    """Generated from proto message testdata.oneof_discriminator.PlainEvent."""
    id: str = ""
    text: Optional[TextContent] = None
    image: Optional[ImageContent] = None

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        d["id"] = self.id
        if self.text is not None:
            d["text"] = self.text.to_dict()
        if self.image is not None:
            d["image"] = self.image.to_dict()
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "PlainEvent":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "id" in data and data["id"] is not None:
            kwargs["id"] = str(data["id"])
        if "text" in data and data["text"] is not None:
            kwargs["text"] = TextContent.from_dict(data["text"])
        if "image" in data and data["image"] is not None:
            kwargs["image"] = ImageContent.from_dict(data["image"])
        return cls(**kwargs)

@dataclass
class TextContent:
    """Generated from proto message testdata.oneof_discriminator.TextContent."""
    body: str = ""

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        d["body"] = self.body
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "TextContent":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "body" in data and data["body"] is not None:
            kwargs["body"] = str(data["body"])
        return cls(**kwargs)

@dataclass
class VideoContent:
    """Generated from proto message testdata.oneof_discriminator.VideoContent."""
    url: str = ""
    duration: int = 0

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        d["url"] = self.url
        d["duration"] = self.duration
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "VideoContent":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "url" in data and data["url"] is not None:
            kwargs["url"] = str(data["url"])
        if "duration" in data and data["duration"] is not None:
            kwargs["duration"] = int(data["duration"])
        return cls(**kwargs)

@dataclass
class OneofDiscriminatorServiceClientOptions:
    """Construct-time options for OneofDiscriminatorServiceClient."""
    transport: Optional[HttpTransport] = None
    default_headers: Optional[Mapping[str, str]] = None
    timeout: Optional[float] = None
    content_type: str = "application/json"
    # Total attempts including the first (1 = no retries). Transport errors and
    # HTTP 429/502/503/504 are retried with exponential backoff. SSE never retries.
    max_retry_attempts: int = 1
    # Delay in seconds before the first retry; doubles on each subsequent retry.
    retry_backoff: float = 0.25


@dataclass
class OneofDiscriminatorServiceCallOptions:
    """Per-call options for OneofDiscriminatorServiceClient methods."""
    headers: Optional[Mapping[str, str]] = None
    timeout: Optional[float] = None
    content_type: Optional[str] = None


class OneofDiscriminatorServiceClient:
    """Generated client for testdata.oneof_discriminator.OneofDiscriminatorService."""
    def __init__(
        self,
        base_url: str,
        options: Optional[OneofDiscriminatorServiceClientOptions] = None,
    ) -> None:
        self._base_url = base_url.rstrip("/")
        opts = options or OneofDiscriminatorServiceClientOptions()
        self._transport: HttpTransport = opts.transport or UrllibTransport()
        self._default_headers: dict[str, str] = dict(opts.default_headers or {})
        self._timeout = opts.timeout
        self._content_type = opts.content_type
        self._max_retry_attempts = opts.max_retry_attempts
        self._retry_backoff = opts.retry_backoff

    def test_flattened_event(
        self,
        req: FlattenedEvent,
        options: Optional[OneofDiscriminatorServiceCallOptions] = None,
    ) -> FlattenedEvent:
        """Calls testdata.oneof_discriminator.OneofDiscriminatorService.TestFlattenedEvent."""
        opts = options or OneofDiscriminatorServiceCallOptions()
        content_type = opts.content_type or self._content_type
        if content_type != "application/json":
            raise NotImplementedError("only application/json is implemented")
        path = "/api/v1/events/flattened"
        headers: dict[str, str] = dict(self._default_headers)
        headers["Content-Type"] = content_type
        headers["Accept"] = "application/json"
        if opts.headers:
            headers.update(opts.headers)
        body = json.dumps(req.to_dict()).encode("utf-8")
        resp = self._request_with_retries(
            method="POST",
            url=self._base_url + path,
            headers=headers,
            body=body,
            timeout=opts.timeout if opts.timeout is not None else self._timeout,
        )
        if resp.status >= 400:
            self._raise_for_status(resp)
        if not resp.body:
            return FlattenedEvent()
        return FlattenedEvent.from_dict(json.loads(resp.body))

    def test_nested_event(
        self,
        req: NestedEvent,
        options: Optional[OneofDiscriminatorServiceCallOptions] = None,
    ) -> NestedEvent:
        """Calls testdata.oneof_discriminator.OneofDiscriminatorService.TestNestedEvent."""
        opts = options or OneofDiscriminatorServiceCallOptions()
        content_type = opts.content_type or self._content_type
        if content_type != "application/json":
            raise NotImplementedError("only application/json is implemented")
        path = "/api/v1/events/nested"
        headers: dict[str, str] = dict(self._default_headers)
        headers["Content-Type"] = content_type
        headers["Accept"] = "application/json"
        if opts.headers:
            headers.update(opts.headers)
        body = json.dumps(req.to_dict()).encode("utf-8")
        resp = self._request_with_retries(
            method="POST",
            url=self._base_url + path,
            headers=headers,
            body=body,
            timeout=opts.timeout if opts.timeout is not None else self._timeout,
        )
        if resp.status >= 400:
            self._raise_for_status(resp)
        if not resp.body:
            return NestedEvent()
        return NestedEvent.from_dict(json.loads(resp.body))

    def test_plain_event(
        self,
        req: PlainEvent,
        options: Optional[OneofDiscriminatorServiceCallOptions] = None,
    ) -> PlainEvent:
        """Calls testdata.oneof_discriminator.OneofDiscriminatorService.TestPlainEvent."""
        opts = options or OneofDiscriminatorServiceCallOptions()
        content_type = opts.content_type or self._content_type
        if content_type != "application/json":
            raise NotImplementedError("only application/json is implemented")
        path = "/api/v1/events/plain"
        headers: dict[str, str] = dict(self._default_headers)
        headers["Content-Type"] = content_type
        headers["Accept"] = "application/json"
        if opts.headers:
            headers.update(opts.headers)
        body = json.dumps(req.to_dict()).encode("utf-8")
        resp = self._request_with_retries(
            method="POST",
            url=self._base_url + path,
            headers=headers,
            body=body,
            timeout=opts.timeout if opts.timeout is not None else self._timeout,
        )
        if resp.status >= 400:
            self._raise_for_status(resp)
        if not resp.body:
            return PlainEvent()
        return PlainEvent.from_dict(json.loads(resp.body))

    def _request_with_retries(
        self,
        method: str,
        url: str,
        headers: Mapping[str, str],
        body: Optional[bytes],
        timeout: Optional[float],
    ) -> HttpResponse:
        """Executes a request, retrying transport errors and 429/502/503/504."""
        attempts = max(1, self._max_retry_attempts)
        last_exc: Optional[Exception] = None
        for attempt in range(attempts):
            if attempt > 0:
                time.sleep(self._retry_backoff * (2 ** (attempt - 1)))
            try:
                resp = self._transport.request(
                    method=method, url=url, headers=headers, body=body, timeout=timeout,
                )
            except Exception as exc:  # noqa: BLE001 - transport errors vary by implementation
                last_exc = exc
                continue
            if attempt < attempts - 1 and resp.status in (429, 502, 503, 504):
                continue
            return resp
        assert last_exc is not None
        raise last_exc

    def _raise_for_status(self, resp: HttpResponse) -> None:
        """Map a non-2xx response to the most specific exception available."""
        body = resp.body or b""
        parsed: Any = None
        ctype = (resp.headers or {}).get("Content-Type", "")
        looks_jsonish = "json" in ctype.lower() or body[:1] in (b"{", b"[")
        if looks_jsonish:
            try:
                parsed = json.loads(body.decode("utf-8"))
            except (ValueError, UnicodeDecodeError):
                parsed = None
        if resp.status == 400 and isinstance(parsed, dict) and "violations" in parsed:
            violations = [
                FieldViolation(field=v.get("field", ""), description=v.get("description", ""))
                for v in parsed.get("violations", [])
            ]
            raise ValidationError(resp.status, body, resp.headers, violations)
        if isinstance(parsed, dict):
            for err_cls, required_keys in _ERROR_CLASSES:
                if required_keys and required_keys.issubset(parsed.keys()):
                    raise err_cls.populate(resp.status, body, resp.headers, parsed)
        raise ApiError(resp.status, body, resp.headers)

