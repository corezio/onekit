# Code generated by protoc-gen-onekit-py-client. DO NOT EDIT.
# source: query_params.proto

from __future__ import annotations

import base64
import binascii
import json
import time
import urllib.error
import urllib.parse
import urllib.request
from dataclasses import dataclass, field
from datetime import datetime, timezone
from enum import IntEnum
from typing import Any, AsyncIterator, Iterator, Mapping, Optional, Protocol, Sequence, Union

@dataclass
class HttpResponse:
    """Minimal HTTP response shape returned by every HttpTransport."""
    status: int
    headers: Mapping[str, str]
    body: bytes


class HttpTransport(Protocol):
    """Duck-typed HTTP transport. Implement this to plug in requests/httpx/aiohttp."""
    def request(
        self,
        method: str,
        url: str,
        headers: Mapping[str, str],
        body: Optional[bytes],
        timeout: Optional[float],
    ) -> HttpResponse: ...


class UrllibTransport:
    """Default transport built on the Python standard library."""
    def request(
        self,
        method: str,
        url: str,
        headers: Mapping[str, str],
        body: Optional[bytes],
        timeout: Optional[float],
    ) -> HttpResponse:
        req = urllib.request.Request(url=url, method=method, data=body)
        for key, value in headers.items():
            req.add_header(key, value)
        try:
            with urllib.request.urlopen(req, timeout=timeout) as resp:
                return HttpResponse(
                    status=resp.status,
                    headers={k: v for k, v in resp.headers.items()},
                    body=resp.read(),
                )
        except urllib.error.HTTPError as exc:
            return HttpResponse(
                status=exc.code,
                headers={k: v for k, v in exc.headers.items()} if exc.headers else {},
                body=exc.read() if hasattr(exc, "read") else b"",
            )


class Region(IntEnum):
    """Generated from proto enum test.httpgen.query.Region."""
    REGION_UNSPECIFIED = 0
    REGION_AMERICAS = 1
    REGION_EUROPE = 2
    REGION_ASIA = 3


Region_JSON_VALUES: Mapping[Region, str] = {
    Region.REGION_UNSPECIFIED: "unspecified",
    Region.REGION_AMERICAS: "americas",
    Region.REGION_EUROPE: "europe",
    Region.REGION_ASIA: "asia",
}

def _decode_enum_Region(value: Any) -> Region:
    if isinstance(value, int):
        return Region(value)
    if isinstance(value, str):
        for member, json_value in Region_JSON_VALUES.items():
            if json_value == value:
                return member
        try:
            return Region[value]
        except KeyError:
            raise ValueError(f"unknown Region value: {value!r}")
    raise TypeError(f"cannot decode Region from {type(value).__name__}")


@dataclass
class FieldViolation:
    """Single validation violation, matching onekit.http.FieldViolation."""
    field: str
    description: str = ""


class ApiError(Exception):
    """Base exception for any non-2xx HTTP response."""
    def __init__(
        self,
        status: int,
        body: bytes,
        headers: Optional[Mapping[str, str]] = None,
    ) -> None:
        self.status = status
        self.body = body
        self.headers = headers or {}
        super().__init__(f"HTTP {status}")


class ValidationError(ApiError):
    """Raised on HTTP 400 when the server returns onekit.http.ValidationError JSON."""
    def __init__(
        self,
        status: int,
        body: bytes,
        headers: Optional[Mapping[str, str]] = None,
        violations: Optional[Sequence[FieldViolation]] = None,
    ) -> None:
        super().__init__(status, body, headers)
        self.violations: list[FieldViolation] = list(violations or [])


_ERROR_CLASSES: list[tuple[type[ApiError], set[str]]] = [
]


@dataclass
class EmptyRequest:
    """Generated from proto message test.httpgen.query.EmptyRequest."""

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "EmptyRequest":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        return cls(**kwargs)

@dataclass
class GetWithFiltersRequest:
    """Generated from proto message test.httpgen.query.GetWithFiltersRequest."""
    resource_id: str = ""
    filter: str = ""
    limit: int = 0

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        d["resourceId"] = self.resource_id
        d["filter"] = self.filter
        d["limit"] = self.limit
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "GetWithFiltersRequest":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "resourceId" in data and data["resourceId"] is not None:
            kwargs["resource_id"] = str(data["resourceId"])
        if "filter" in data and data["filter"] is not None:
            kwargs["filter"] = str(data["filter"])
        if "limit" in data and data["limit"] is not None:
            kwargs["limit"] = int(data["limit"])
        return cls(**kwargs)

@dataclass
class SearchAdvancedRequest:
    """Generated from proto message test.httpgen.query.SearchAdvancedRequest."""
    region: Region = Region.REGION_UNSPECIFIED
    countries: list[str] = field(default_factory=list)
    keyword: str = ""
    years: list[int] = field(default_factory=list)
    flags: list[bool] = field(default_factory=list)

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        d["region"] = Region_JSON_VALUES.get(self.region, self.region.name)
        if self.countries:
            d["countries"] = [v for v in self.countries]
        d["keyword"] = self.keyword
        if self.years:
            d["years"] = [v for v in self.years]
        if self.flags:
            d["flags"] = [v for v in self.flags]
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "SearchAdvancedRequest":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "region" in data and data["region"] is not None:
            kwargs["region"] = _decode_enum_Region(data["region"])
        if "countries" in data and data["countries"] is not None:
            kwargs["countries"] = [str(v) for v in data["countries"]]
        if "keyword" in data and data["keyword"] is not None:
            kwargs["keyword"] = str(data["keyword"])
        if "years" in data and data["years"] is not None:
            kwargs["years"] = [int(v) for v in data["years"]]
        if "flags" in data and data["flags"] is not None:
            kwargs["flags"] = [bool(v) for v in data["flags"]]
        return cls(**kwargs)

@dataclass
class SearchCustomNamesRequest:
    """Generated from proto message test.httpgen.query.SearchCustomNamesRequest."""
    search_term: str = ""
    results_per_page: int = 0
    page_number: int = 0
    sort_field: str = ""
    descending_order: bool = False

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        d["searchTerm"] = self.search_term
        d["resultsPerPage"] = self.results_per_page
        d["pageNumber"] = self.page_number
        d["sortField"] = self.sort_field
        d["descendingOrder"] = self.descending_order
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "SearchCustomNamesRequest":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "searchTerm" in data and data["searchTerm"] is not None:
            kwargs["search_term"] = str(data["searchTerm"])
        if "resultsPerPage" in data and data["resultsPerPage"] is not None:
            kwargs["results_per_page"] = int(data["resultsPerPage"])
        if "pageNumber" in data and data["pageNumber"] is not None:
            kwargs["page_number"] = int(data["pageNumber"])
        if "sortField" in data and data["sortField"] is not None:
            kwargs["sort_field"] = str(data["sortField"])
        if "descendingOrder" in data and data["descendingOrder"] is not None:
            kwargs["descending_order"] = bool(data["descendingOrder"])
        return cls(**kwargs)

@dataclass
class SearchRequiredRequest:
    """Generated from proto message test.httpgen.query.SearchRequiredRequest."""
    query: str = ""
    page: int = 0
    page_size: int = 0

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        d["query"] = self.query
        d["page"] = self.page
        d["pageSize"] = self.page_size
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "SearchRequiredRequest":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "query" in data and data["query"] is not None:
            kwargs["query"] = str(data["query"])
        if "page" in data and data["page"] is not None:
            kwargs["page"] = int(data["page"])
        if "pageSize" in data and data["pageSize"] is not None:
            kwargs["page_size"] = int(data["pageSize"])
        return cls(**kwargs)

@dataclass
class SearchResponse:
    """Generated from proto message test.httpgen.query.SearchResponse."""
    results: list[str] = field(default_factory=list)
    total: int = 0

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        if self.results:
            d["results"] = [v for v in self.results]
        d["total"] = self.total
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "SearchResponse":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "results" in data and data["results"] is not None:
            kwargs["results"] = [str(v) for v in data["results"]]
        if "total" in data and data["total"] is not None:
            kwargs["total"] = int(data["total"])
        return cls(**kwargs)

@dataclass
class SearchWithTypesRequest:
    """Generated from proto message test.httpgen.query.SearchWithTypesRequest."""
    query: str = ""
    limit: int = 0
    offset: str = "0"
    active: bool = False
    min_score: float = 0.0
    max_score: float = 0.0
    page: int = 0
    timestamp: str = "0"

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        d["query"] = self.query
        d["limit"] = self.limit
        d["offset"] = str(self.offset)
        d["active"] = self.active
        d["minScore"] = self.min_score
        d["maxScore"] = self.max_score
        d["page"] = self.page
        d["timestamp"] = str(self.timestamp)
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "SearchWithTypesRequest":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "query" in data and data["query"] is not None:
            kwargs["query"] = str(data["query"])
        if "limit" in data and data["limit"] is not None:
            kwargs["limit"] = int(data["limit"])
        if "offset" in data and data["offset"] is not None:
            kwargs["offset"] = str(data["offset"])
        if "active" in data and data["active"] is not None:
            kwargs["active"] = bool(data["active"])
        if "minScore" in data and data["minScore"] is not None:
            kwargs["min_score"] = float(data["minScore"])
        if "maxScore" in data and data["maxScore"] is not None:
            kwargs["max_score"] = float(data["maxScore"])
        if "page" in data and data["page"] is not None:
            kwargs["page"] = int(data["page"])
        if "timestamp" in data and data["timestamp"] is not None:
            kwargs["timestamp"] = str(data["timestamp"])
        return cls(**kwargs)

@dataclass
class QueryParamServiceClientOptions:
    """Construct-time options for QueryParamServiceClient."""
    transport: Optional[HttpTransport] = None
    default_headers: Optional[Mapping[str, str]] = None
    timeout: Optional[float] = None
    content_type: str = "application/json"
    # Total attempts including the first (1 = no retries). Transport errors and
    # HTTP 429/502/503/504 are retried with exponential backoff. SSE never retries.
    max_retry_attempts: int = 1
    # Delay in seconds before the first retry; doubles on each subsequent retry.
    retry_backoff: float = 0.25


@dataclass
class QueryParamServiceCallOptions:
    """Per-call options for QueryParamServiceClient methods."""
    headers: Optional[Mapping[str, str]] = None
    timeout: Optional[float] = None
    content_type: Optional[str] = None


class QueryParamServiceClient:
    """Generated client for test.httpgen.query.QueryParamService."""
    def __init__(
        self,
        base_url: str,
        options: Optional[QueryParamServiceClientOptions] = None,
    ) -> None:
        self._base_url = base_url.rstrip("/")
        opts = options or QueryParamServiceClientOptions()
        self._transport: HttpTransport = opts.transport or UrllibTransport()
        self._default_headers: dict[str, str] = dict(opts.default_headers or {})
        self._timeout = opts.timeout
        self._content_type = opts.content_type
        self._max_retry_attempts = opts.max_retry_attempts
        self._retry_backoff = opts.retry_backoff

    def search_with_types(
        self,
        req: SearchWithTypesRequest,
        options: Optional[QueryParamServiceCallOptions] = None,
    ) -> SearchResponse:
        """Calls test.httpgen.query.QueryParamService.SearchWithTypes."""
        opts = options or QueryParamServiceCallOptions()
        content_type = opts.content_type or self._content_type
        if content_type != "application/json":
            raise NotImplementedError("only application/json is implemented")
        path = "/api/search/typed"
        query_pairs: list[tuple[str, str]] = []
        if req.query:
            query_pairs.append(("q", str(req.query)))
        if req.limit is not None and req.limit != 0:
            query_pairs.append(("limit", str(req.limit)))
        if req.offset is not None and req.offset != 0:
            query_pairs.append(("offset", str(req.offset)))
        if req.active:
            query_pairs.append(("active", "true" if req.active else "false"))
        if req.min_score is not None and req.min_score != 0:
            query_pairs.append(("min_score", str(req.min_score)))
        if req.max_score is not None and req.max_score != 0:
            query_pairs.append(("max_score", str(req.max_score)))
        if req.page is not None and req.page != 0:
            query_pairs.append(("page", str(req.page)))
        if req.timestamp is not None and req.timestamp != 0:
            query_pairs.append(("ts", str(req.timestamp)))
        if query_pairs:
            path = path + "?" + urllib.parse.urlencode(query_pairs, doseq=True)
        headers: dict[str, str] = dict(self._default_headers)
        headers["Content-Type"] = content_type
        headers["Accept"] = "application/json"
        if opts.headers:
            headers.update(opts.headers)
        body: Optional[bytes] = None
        resp = self._request_with_retries(
            method="GET",
            url=self._base_url + path,
            headers=headers,
            body=body,
            timeout=opts.timeout if opts.timeout is not None else self._timeout,
        )
        if resp.status >= 400:
            self._raise_for_status(resp)
        if not resp.body:
            return SearchResponse()
        return SearchResponse.from_dict(json.loads(resp.body))

    def search_required(
        self,
        req: SearchRequiredRequest,
        options: Optional[QueryParamServiceCallOptions] = None,
    ) -> SearchResponse:
        """Calls test.httpgen.query.QueryParamService.SearchRequired."""
        opts = options or QueryParamServiceCallOptions()
        content_type = opts.content_type or self._content_type
        if content_type != "application/json":
            raise NotImplementedError("only application/json is implemented")
        path = "/api/search/required"
        query_pairs: list[tuple[str, str]] = []
        if req.query:
            query_pairs.append(("q", str(req.query)))
        if req.page is not None and req.page != 0:
            query_pairs.append(("page", str(req.page)))
        if req.page_size is not None and req.page_size != 0:
            query_pairs.append(("page_size", str(req.page_size)))
        if query_pairs:
            path = path + "?" + urllib.parse.urlencode(query_pairs, doseq=True)
        headers: dict[str, str] = dict(self._default_headers)
        headers["Content-Type"] = content_type
        headers["Accept"] = "application/json"
        if opts.headers:
            headers.update(opts.headers)
        body: Optional[bytes] = None
        resp = self._request_with_retries(
            method="GET",
            url=self._base_url + path,
            headers=headers,
            body=body,
            timeout=opts.timeout if opts.timeout is not None else self._timeout,
        )
        if resp.status >= 400:
            self._raise_for_status(resp)
        if not resp.body:
            return SearchResponse()
        return SearchResponse.from_dict(json.loads(resp.body))

    def search_custom_names(
        self,
        req: SearchCustomNamesRequest,
        options: Optional[QueryParamServiceCallOptions] = None,
    ) -> SearchResponse:
        """Calls test.httpgen.query.QueryParamService.SearchCustomNames."""
        opts = options or QueryParamServiceCallOptions()
        content_type = opts.content_type or self._content_type
        if content_type != "application/json":
            raise NotImplementedError("only application/json is implemented")
        path = "/api/search/custom"
        query_pairs: list[tuple[str, str]] = []
        if req.search_term:
            query_pairs.append(("q", str(req.search_term)))
        if req.results_per_page is not None and req.results_per_page != 0:
            query_pairs.append(("limit", str(req.results_per_page)))
        if req.page_number is not None and req.page_number != 0:
            query_pairs.append(("page", str(req.page_number)))
        if req.sort_field:
            query_pairs.append(("sort", str(req.sort_field)))
        if req.descending_order:
            query_pairs.append(("desc", "true" if req.descending_order else "false"))
        if query_pairs:
            path = path + "?" + urllib.parse.urlencode(query_pairs, doseq=True)
        headers: dict[str, str] = dict(self._default_headers)
        headers["Content-Type"] = content_type
        headers["Accept"] = "application/json"
        if opts.headers:
            headers.update(opts.headers)
        body: Optional[bytes] = None
        resp = self._request_with_retries(
            method="GET",
            url=self._base_url + path,
            headers=headers,
            body=body,
            timeout=opts.timeout if opts.timeout is not None else self._timeout,
        )
        if resp.status >= 400:
            self._raise_for_status(resp)
        if not resp.body:
            return SearchResponse()
        return SearchResponse.from_dict(json.loads(resp.body))

    def get_with_filters(
        self,
        req: GetWithFiltersRequest,
        options: Optional[QueryParamServiceCallOptions] = None,
    ) -> SearchResponse:
        """Calls test.httpgen.query.QueryParamService.GetWithFilters."""
        opts = options or QueryParamServiceCallOptions()
        content_type = opts.content_type or self._content_type
        if content_type != "application/json":
            raise NotImplementedError("only application/json is implemented")
        path = "/api/resources/{resource_id}/items"
        path = path.replace("{resource_id}", urllib.parse.quote(str(req.resource_id), safe=""))
        query_pairs: list[tuple[str, str]] = []
        if req.filter:
            query_pairs.append(("filter", str(req.filter)))
        if req.limit is not None and req.limit != 0:
            query_pairs.append(("limit", str(req.limit)))
        if query_pairs:
            path = path + "?" + urllib.parse.urlencode(query_pairs, doseq=True)
        headers: dict[str, str] = dict(self._default_headers)
        headers["Content-Type"] = content_type
        headers["Accept"] = "application/json"
        if opts.headers:
            headers.update(opts.headers)
        body: Optional[bytes] = None
        resp = self._request_with_retries(
            method="GET",
            url=self._base_url + path,
            headers=headers,
            body=body,
            timeout=opts.timeout if opts.timeout is not None else self._timeout,
        )
        if resp.status >= 400:
            self._raise_for_status(resp)
        if not resp.body:
            return SearchResponse()
        return SearchResponse.from_dict(json.loads(resp.body))

    def search_advanced(
        self,
        req: SearchAdvancedRequest,
        options: Optional[QueryParamServiceCallOptions] = None,
    ) -> SearchResponse:
        """Calls test.httpgen.query.QueryParamService.SearchAdvanced."""
        opts = options or QueryParamServiceCallOptions()
        content_type = opts.content_type or self._content_type
        if content_type != "application/json":
            raise NotImplementedError("only application/json is implemented")
        path = "/api/search/advanced"
        query_pairs: list[tuple[str, str]] = []
        if req.region is not None and req.region != 0:
            query_pairs.append(("region", str(req.region)))
        if req.countries:
            for _v in req.countries:
                query_pairs.append(("countries", str(_v)))
        if req.keyword:
            query_pairs.append(("keyword", str(req.keyword)))
        if req.years:
            for _v in req.years:
                query_pairs.append(("years", str(_v)))
        if req.flags:
            for _v in req.flags:
                query_pairs.append(("flags", str(_v)))
        if query_pairs:
            path = path + "?" + urllib.parse.urlencode(query_pairs, doseq=True)
        headers: dict[str, str] = dict(self._default_headers)
        headers["Content-Type"] = content_type
        headers["Accept"] = "application/json"
        if opts.headers:
            headers.update(opts.headers)
        body: Optional[bytes] = None
        resp = self._request_with_retries(
            method="GET",
            url=self._base_url + path,
            headers=headers,
            body=body,
            timeout=opts.timeout if opts.timeout is not None else self._timeout,
        )
        if resp.status >= 400:
            self._raise_for_status(resp)
        if not resp.body:
            return SearchResponse()
        return SearchResponse.from_dict(json.loads(resp.body))

    def get_defaults(
        self,
        req: EmptyRequest,
        options: Optional[QueryParamServiceCallOptions] = None,
    ) -> SearchResponse:
        """Calls test.httpgen.query.QueryParamService.GetDefaults."""
        opts = options or QueryParamServiceCallOptions()
        content_type = opts.content_type or self._content_type
        if content_type != "application/json":
            raise NotImplementedError("only application/json is implemented")
        path = "/api/defaults"
        headers: dict[str, str] = dict(self._default_headers)
        headers["Content-Type"] = content_type
        headers["Accept"] = "application/json"
        if opts.headers:
            headers.update(opts.headers)
        body: Optional[bytes] = None
        resp = self._request_with_retries(
            method="GET",
            url=self._base_url + path,
            headers=headers,
            body=body,
            timeout=opts.timeout if opts.timeout is not None else self._timeout,
        )
        if resp.status >= 400:
            self._raise_for_status(resp)
        if not resp.body:
            return SearchResponse()
        return SearchResponse.from_dict(json.loads(resp.body))

    def _request_with_retries(
        self,
        method: str,
        url: str,
        headers: Mapping[str, str],
        body: Optional[bytes],
        timeout: Optional[float],
    ) -> HttpResponse:
        """Executes a request, retrying transport errors and 429/502/503/504."""
        attempts = max(1, self._max_retry_attempts)
        last_exc: Optional[Exception] = None
        for attempt in range(attempts):
            if attempt > 0:
                time.sleep(self._retry_backoff * (2 ** (attempt - 1)))
            try:
                resp = self._transport.request(
                    method=method, url=url, headers=headers, body=body, timeout=timeout,
                )
            except Exception as exc:  # noqa: BLE001 - transport errors vary by implementation
                last_exc = exc
                continue
            if attempt < attempts - 1 and resp.status in (429, 502, 503, 504):
                continue
            return resp
        assert last_exc is not None
        raise last_exc

    def _raise_for_status(self, resp: HttpResponse) -> None:
        """Map a non-2xx response to the most specific exception available."""
        body = resp.body or b""
        parsed: Any = None
        ctype = (resp.headers or {}).get("Content-Type", "")
        looks_jsonish = "json" in ctype.lower() or body[:1] in (b"{", b"[")
        if looks_jsonish:
            try:
                parsed = json.loads(body.decode("utf-8"))
            except (ValueError, UnicodeDecodeError):
                parsed = None
        if resp.status == 400 and isinstance(parsed, dict) and "violations" in parsed:
            violations = [
                FieldViolation(field=v.get("field", ""), description=v.get("description", ""))
                for v in parsed.get("violations", [])
            ]
            raise ValidationError(resp.status, body, resp.headers, violations)
        if isinstance(parsed, dict):
            for err_cls, required_keys in _ERROR_CLASSES:
                if required_keys and required_keys.issubset(parsed.keys()):
                    raise err_cls.populate(resp.status, body, resp.headers, parsed)
        raise ApiError(resp.status, body, resp.headers)

