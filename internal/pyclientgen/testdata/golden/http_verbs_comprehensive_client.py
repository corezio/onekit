# Code generated by protoc-gen-onekit-py-client. DO NOT EDIT.
# source: http_verbs_comprehensive.proto

from __future__ import annotations

import base64
import binascii
import json
import time
import urllib.error
import urllib.parse
import urllib.request
from dataclasses import dataclass, field
from datetime import datetime, timezone
from enum import IntEnum
from typing import Any, AsyncIterator, Iterator, Mapping, Optional, Protocol, Sequence, Union

@dataclass
class HttpResponse:
    """Minimal HTTP response shape returned by every HttpTransport."""
    status: int
    headers: Mapping[str, str]
    body: bytes


class HttpTransport(Protocol):
    """Duck-typed HTTP transport. Implement this to plug in requests/httpx/aiohttp."""
    def request(
        self,
        method: str,
        url: str,
        headers: Mapping[str, str],
        body: Optional[bytes],
        timeout: Optional[float],
    ) -> HttpResponse: ...


class UrllibTransport:
    """Default transport built on the Python standard library."""
    def request(
        self,
        method: str,
        url: str,
        headers: Mapping[str, str],
        body: Optional[bytes],
        timeout: Optional[float],
    ) -> HttpResponse:
        req = urllib.request.Request(url=url, method=method, data=body)
        for key, value in headers.items():
            req.add_header(key, value)
        try:
            with urllib.request.urlopen(req, timeout=timeout) as resp:
                return HttpResponse(
                    status=resp.status,
                    headers={k: v for k, v in resp.headers.items()},
                    body=resp.read(),
                )
        except urllib.error.HTTPError as exc:
            return HttpResponse(
                status=exc.code,
                headers={k: v for k, v in exc.headers.items()} if exc.headers else {},
                body=exc.read() if hasattr(exc, "read") else b"",
            )


class ResourceStatus(IntEnum):
    """Generated from proto enum test.httpgen.ResourceStatus."""
    RESOURCE_STATUS_UNSPECIFIED = 0
    RESOURCE_STATUS_ACTIVE = 1
    RESOURCE_STATUS_INACTIVE = 2
    RESOURCE_STATUS_ARCHIVED = 3


ResourceStatus_JSON_VALUES: Mapping[ResourceStatus, str] = {}

def _decode_enum_ResourceStatus(value: Any) -> ResourceStatus:
    if isinstance(value, int):
        return ResourceStatus(value)
    if isinstance(value, str):
        for member, json_value in ResourceStatus_JSON_VALUES.items():
            if json_value == value:
                return member
        try:
            return ResourceStatus[value]
        except KeyError:
            raise ValueError(f"unknown ResourceStatus value: {value!r}")
    raise TypeError(f"cannot decode ResourceStatus from {type(value).__name__}")


@dataclass
class FieldViolation:
    """Single validation violation, matching onekit.http.FieldViolation."""
    field: str
    description: str = ""


class ApiError(Exception):
    """Base exception for any non-2xx HTTP response."""
    def __init__(
        self,
        status: int,
        body: bytes,
        headers: Optional[Mapping[str, str]] = None,
    ) -> None:
        self.status = status
        self.body = body
        self.headers = headers or {}
        super().__init__(f"HTTP {status}")


class ValidationError(ApiError):
    """Raised on HTTP 400 when the server returns onekit.http.ValidationError JSON."""
    def __init__(
        self,
        status: int,
        body: bytes,
        headers: Optional[Mapping[str, str]] = None,
        violations: Optional[Sequence[FieldViolation]] = None,
    ) -> None:
        super().__init__(status, body, headers)
        self.violations: list[FieldViolation] = list(violations or [])


_ERROR_CLASSES: list[tuple[type[ApiError], set[str]]] = [
]


@dataclass
class CreateResourceRequest:
    """Generated from proto message test.httpgen.CreateResourceRequest."""
    name: str = ""
    description: str = ""
    metadata: dict[str, str] = field(default_factory=dict)

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        d["name"] = self.name
        d["description"] = self.description
        if self.metadata:
            d["metadata"] = {k: v for k, v in self.metadata.items()}
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "CreateResourceRequest":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "name" in data and data["name"] is not None:
            kwargs["name"] = str(data["name"])
        if "description" in data and data["description"] is not None:
            kwargs["description"] = str(data["description"])
        if "metadata" in data and data["metadata"] is not None:
            kwargs["metadata"] = {k: str(v) for k, v in data["metadata"].items()}
        return cls(**kwargs)

@dataclass
class DefaultPostRequest:
    """Generated from proto message test.httpgen.DefaultPostRequest."""
    action: str = ""

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        d["action"] = self.action
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "DefaultPostRequest":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "action" in data and data["action"] is not None:
            kwargs["action"] = str(data["action"])
        return cls(**kwargs)

@dataclass
class DefaultPostResponse:
    """Generated from proto message test.httpgen.DefaultPostResponse."""
    result: str = ""

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        d["result"] = self.result
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "DefaultPostResponse":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "result" in data and data["result"] is not None:
            kwargs["result"] = str(data["result"])
        return cls(**kwargs)

@dataclass
class DeleteResourceRequest:
    """Generated from proto message test.httpgen.DeleteResourceRequest."""
    resource_id: str = ""

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        d["resourceId"] = self.resource_id
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "DeleteResourceRequest":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "resourceId" in data and data["resourceId"] is not None:
            kwargs["resource_id"] = str(data["resourceId"])
        return cls(**kwargs)

@dataclass
class DeleteResourceResponse:
    """Generated from proto message test.httpgen.DeleteResourceResponse."""
    success: bool = False

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        d["success"] = self.success
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "DeleteResourceResponse":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "success" in data and data["success"] is not None:
            kwargs["success"] = bool(data["success"])
        return cls(**kwargs)

@dataclass
class GetNestedResourceRequest:
    """Generated from proto message test.httpgen.GetNestedResourceRequest."""
    org_id: str = ""
    team_id: str = ""
    resource_id: str = ""

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        d["orgId"] = self.org_id
        d["teamId"] = self.team_id
        d["resourceId"] = self.resource_id
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "GetNestedResourceRequest":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "orgId" in data and data["orgId"] is not None:
            kwargs["org_id"] = str(data["orgId"])
        if "teamId" in data and data["teamId"] is not None:
            kwargs["team_id"] = str(data["teamId"])
        if "resourceId" in data and data["resourceId"] is not None:
            kwargs["resource_id"] = str(data["resourceId"])
        return cls(**kwargs)

@dataclass
class GetResourceRequest:
    """Generated from proto message test.httpgen.GetResourceRequest."""
    resource_id: str = ""

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        d["resourceId"] = self.resource_id
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "GetResourceRequest":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "resourceId" in data and data["resourceId"] is not None:
            kwargs["resource_id"] = str(data["resourceId"])
        return cls(**kwargs)

@dataclass
class LegacyRequest:
    """Generated from proto message test.httpgen.LegacyRequest."""
    data: str = ""

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        d["data"] = self.data
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "LegacyRequest":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "data" in data and data["data"] is not None:
            kwargs["data"] = str(data["data"])
        return cls(**kwargs)

@dataclass
class LegacyResponse:
    """Generated from proto message test.httpgen.LegacyResponse."""
    result: str = ""

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        d["result"] = self.result
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "LegacyResponse":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "result" in data and data["result"] is not None:
            kwargs["result"] = str(data["result"])
        return cls(**kwargs)

@dataclass
class ListResourcesRequest:
    """Generated from proto message test.httpgen.ListResourcesRequest."""
    page: int = 0
    page_size: int = 0
    filter: str = ""
    include_deleted: bool = False
    since_timestamp: str = "0"
    max_id: str = "0"
    min_score: float = 0.0
    max_score: float = 0.0

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        d["page"] = self.page
        d["pageSize"] = self.page_size
        d["filter"] = self.filter
        d["includeDeleted"] = self.include_deleted
        d["sinceTimestamp"] = str(self.since_timestamp)
        d["maxId"] = str(self.max_id)
        d["minScore"] = self.min_score
        d["maxScore"] = self.max_score
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "ListResourcesRequest":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "page" in data and data["page"] is not None:
            kwargs["page"] = int(data["page"])
        if "pageSize" in data and data["pageSize"] is not None:
            kwargs["page_size"] = int(data["pageSize"])
        if "filter" in data and data["filter"] is not None:
            kwargs["filter"] = str(data["filter"])
        if "includeDeleted" in data and data["includeDeleted"] is not None:
            kwargs["include_deleted"] = bool(data["includeDeleted"])
        if "sinceTimestamp" in data and data["sinceTimestamp"] is not None:
            kwargs["since_timestamp"] = str(data["sinceTimestamp"])
        if "maxId" in data and data["maxId"] is not None:
            kwargs["max_id"] = str(data["maxId"])
        if "minScore" in data and data["minScore"] is not None:
            kwargs["min_score"] = float(data["minScore"])
        if "maxScore" in data and data["maxScore"] is not None:
            kwargs["max_score"] = float(data["maxScore"])
        return cls(**kwargs)

@dataclass
class ListResourcesResponse:
    """Generated from proto message test.httpgen.ListResourcesResponse."""
    resources: list[Resource] = field(default_factory=list)
    total_count: int = 0
    page: int = 0

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        if self.resources:
            d["resources"] = [v.to_dict() for v in self.resources]
        d["totalCount"] = self.total_count
        d["page"] = self.page
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "ListResourcesResponse":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "resources" in data and data["resources"] is not None:
            kwargs["resources"] = [Resource.from_dict(v) for v in data["resources"]]
        if "totalCount" in data and data["totalCount"] is not None:
            kwargs["total_count"] = int(data["totalCount"])
        if "page" in data and data["page"] is not None:
            kwargs["page"] = int(data["page"])
        return cls(**kwargs)

@dataclass
class PatchResourceRequest:
    """Generated from proto message test.httpgen.PatchResourceRequest."""
    resource_id: str = ""
    name: str = ""
    description: str = ""

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        d["resourceId"] = self.resource_id
        d["name"] = self.name
        d["description"] = self.description
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "PatchResourceRequest":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "resourceId" in data and data["resourceId"] is not None:
            kwargs["resource_id"] = str(data["resourceId"])
        if "name" in data and data["name"] is not None:
            kwargs["name"] = str(data["name"])
        if "description" in data and data["description"] is not None:
            kwargs["description"] = str(data["description"])
        return cls(**kwargs)

@dataclass
class Resource:
    """Generated from proto message test.httpgen.Resource."""
    id: str = ""
    name: str = ""
    description: str = ""
    metadata: dict[str, str] = field(default_factory=dict)
    created_at: str = "0"
    updated_at: str = "0"
    status: ResourceStatus = ResourceStatus.RESOURCE_STATUS_UNSPECIFIED
    metadata_detail: Optional[ResourceMetadata] = None
    tag: Optional[str] = None

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        d["id"] = self.id
        d["name"] = self.name
        d["description"] = self.description
        if self.metadata:
            d["metadata"] = {k: v for k, v in self.metadata.items()}
        d["createdAt"] = str(self.created_at)
        d["updatedAt"] = str(self.updated_at)
        d["status"] = ResourceStatus_JSON_VALUES.get(self.status, self.status.name)
        if self.metadata_detail is not None:
            d["metadataDetail"] = self.metadata_detail.to_dict()
        if self.tag is not None:
            d["tag"] = self.tag
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "Resource":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "id" in data and data["id"] is not None:
            kwargs["id"] = str(data["id"])
        if "name" in data and data["name"] is not None:
            kwargs["name"] = str(data["name"])
        if "description" in data and data["description"] is not None:
            kwargs["description"] = str(data["description"])
        if "metadata" in data and data["metadata"] is not None:
            kwargs["metadata"] = {k: str(v) for k, v in data["metadata"].items()}
        if "createdAt" in data and data["createdAt"] is not None:
            kwargs["created_at"] = str(data["createdAt"])
        if "updatedAt" in data and data["updatedAt"] is not None:
            kwargs["updated_at"] = str(data["updatedAt"])
        if "status" in data and data["status"] is not None:
            kwargs["status"] = _decode_enum_ResourceStatus(data["status"])
        if "metadataDetail" in data and data["metadataDetail"] is not None:
            kwargs["metadata_detail"] = ResourceMetadata.from_dict(data["metadataDetail"])
        if "tag" in data and data["tag"] is not None:
            kwargs["tag"] = str(data["tag"])
        return cls(**kwargs)

@dataclass
class ResourceMetadata:
    """Generated from proto message test.httpgen.ResourceMetadata."""
    created_by: str = ""
    created_at_unix: str = "0"
    version: int = 0

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        d["createdBy"] = self.created_by
        d["createdAtUnix"] = str(self.created_at_unix)
        d["version"] = self.version
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "ResourceMetadata":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "createdBy" in data and data["createdBy"] is not None:
            kwargs["created_by"] = str(data["createdBy"])
        if "createdAtUnix" in data and data["createdAtUnix"] is not None:
            kwargs["created_at_unix"] = str(data["createdAtUnix"])
        if "version" in data and data["version"] is not None:
            kwargs["version"] = int(data["version"])
        return cls(**kwargs)

@dataclass
class SearchResourcesRequest:
    """Generated from proto message test.httpgen.SearchResourcesRequest."""
    status_filter: ResourceStatus = ResourceStatus.RESOURCE_STATUS_UNSPECIFIED
    query: str = ""

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        d["statusFilter"] = ResourceStatus_JSON_VALUES.get(self.status_filter, self.status_filter.name)
        d["query"] = self.query
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "SearchResourcesRequest":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "statusFilter" in data and data["statusFilter"] is not None:
            kwargs["status_filter"] = _decode_enum_ResourceStatus(data["statusFilter"])
        if "query" in data and data["query"] is not None:
            kwargs["query"] = str(data["query"])
        return cls(**kwargs)

@dataclass
class UpdateResourceRequest:
    """Generated from proto message test.httpgen.UpdateResourceRequest."""
    resource_id: str = ""
    name: str = ""
    description: str = ""
    metadata: dict[str, str] = field(default_factory=dict)

    def to_dict(self) -> Any:
        """Serialize to a JSON-ready dict respecting onekit JSON mapping annotations."""
        d: dict[str, Any] = {}
        d["resourceId"] = self.resource_id
        d["name"] = self.name
        d["description"] = self.description
        if self.metadata:
            d["metadata"] = {k: v for k, v in self.metadata.items()}
        return d

    @classmethod
    def from_dict(cls, data: Any) -> "UpdateResourceRequest":
        """Deserialize from a JSON-decoded dict (or value, for root-unwrapped messages)."""
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "resourceId" in data and data["resourceId"] is not None:
            kwargs["resource_id"] = str(data["resourceId"])
        if "name" in data and data["name"] is not None:
            kwargs["name"] = str(data["name"])
        if "description" in data and data["description"] is not None:
            kwargs["description"] = str(data["description"])
        if "metadata" in data and data["metadata"] is not None:
            kwargs["metadata"] = {k: str(v) for k, v in data["metadata"].items()}
        return cls(**kwargs)

@dataclass
class RESTfulAPIServiceClientOptions:
    """Construct-time options for RESTfulAPIServiceClient."""
    transport: Optional[HttpTransport] = None
    default_headers: Optional[Mapping[str, str]] = None
    timeout: Optional[float] = None
    content_type: str = "application/json"
    # Total attempts including the first (1 = no retries). Transport errors and
    # HTTP 429/502/503/504 are retried with exponential backoff. SSE never retries.
    max_retry_attempts: int = 1
    # Delay in seconds before the first retry; doubles on each subsequent retry.
    retry_backoff: float = 0.25
    api_key: Optional[str] = None


@dataclass
class RESTfulAPIServiceCallOptions:
    """Per-call options for RESTfulAPIServiceClient methods."""
    headers: Optional[Mapping[str, str]] = None
    timeout: Optional[float] = None
    content_type: Optional[str] = None
    api_key: Optional[str] = None
    request_id: Optional[str] = None


class RESTfulAPIServiceClient:
    """Generated client for test.httpgen.RESTfulAPIService."""
    def __init__(
        self,
        base_url: str,
        options: Optional[RESTfulAPIServiceClientOptions] = None,
    ) -> None:
        self._base_url = base_url.rstrip("/")
        opts = options or RESTfulAPIServiceClientOptions()
        self._transport: HttpTransport = opts.transport or UrllibTransport()
        self._default_headers: dict[str, str] = dict(opts.default_headers or {})
        self._timeout = opts.timeout
        self._content_type = opts.content_type
        self._max_retry_attempts = opts.max_retry_attempts
        self._retry_backoff = opts.retry_backoff
        if opts.api_key is not None:
            self._default_headers["X-API-Key"] = opts.api_key

    def list_resources(
        self,
        req: ListResourcesRequest,
        options: Optional[RESTfulAPIServiceCallOptions] = None,
    ) -> ListResourcesResponse:
        """Calls test.httpgen.RESTfulAPIService.ListResources."""
        opts = options or RESTfulAPIServiceCallOptions()
        content_type = opts.content_type or self._content_type
        if content_type != "application/json":
            raise NotImplementedError("only application/json is implemented")
        path = "/api/v1/resources"
        query_pairs: list[tuple[str, str]] = []
        if req.page is not None and req.page != 0:
            query_pairs.append(("page", str(req.page)))
        if req.page_size is not None and req.page_size != 0:
            query_pairs.append(("page_size", str(req.page_size)))
        if req.filter:
            query_pairs.append(("filter", str(req.filter)))
        if req.include_deleted:
            query_pairs.append(("include_deleted", "true" if req.include_deleted else "false"))
        if req.since_timestamp is not None and req.since_timestamp != 0:
            query_pairs.append(("since_timestamp", str(req.since_timestamp)))
        if req.max_id is not None and req.max_id != 0:
            query_pairs.append(("max_id", str(req.max_id)))
        if req.min_score is not None and req.min_score != 0:
            query_pairs.append(("min_score", str(req.min_score)))
        if req.max_score is not None and req.max_score != 0:
            query_pairs.append(("max_score", str(req.max_score)))
        if query_pairs:
            path = path + "?" + urllib.parse.urlencode(query_pairs, doseq=True)
        headers: dict[str, str] = dict(self._default_headers)
        headers["Content-Type"] = content_type
        headers["Accept"] = "application/json"
        if opts.headers:
            headers.update(opts.headers)
        if opts.api_key is not None:
            headers["X-API-Key"] = opts.api_key
        body: Optional[bytes] = None
        resp = self._request_with_retries(
            method="GET",
            url=self._base_url + path,
            headers=headers,
            body=body,
            timeout=opts.timeout if opts.timeout is not None else self._timeout,
        )
        if resp.status >= 400:
            self._raise_for_status(resp)
        if not resp.body:
            return ListResourcesResponse()
        return ListResourcesResponse.from_dict(json.loads(resp.body))

    def get_resource(
        self,
        req: GetResourceRequest,
        options: Optional[RESTfulAPIServiceCallOptions] = None,
    ) -> Resource:
        """Calls test.httpgen.RESTfulAPIService.GetResource."""
        opts = options or RESTfulAPIServiceCallOptions()
        content_type = opts.content_type or self._content_type
        if content_type != "application/json":
            raise NotImplementedError("only application/json is implemented")
        path = "/api/v1/resources/{resource_id}"
        path = path.replace("{resource_id}", urllib.parse.quote(str(req.resource_id), safe=""))
        headers: dict[str, str] = dict(self._default_headers)
        headers["Content-Type"] = content_type
        headers["Accept"] = "application/json"
        if opts.headers:
            headers.update(opts.headers)
        if opts.api_key is not None:
            headers["X-API-Key"] = opts.api_key
        body: Optional[bytes] = None
        resp = self._request_with_retries(
            method="GET",
            url=self._base_url + path,
            headers=headers,
            body=body,
            timeout=opts.timeout if opts.timeout is not None else self._timeout,
        )
        if resp.status >= 400:
            self._raise_for_status(resp)
        if not resp.body:
            return Resource()
        return Resource.from_dict(json.loads(resp.body))

    def get_nested_resource(
        self,
        req: GetNestedResourceRequest,
        options: Optional[RESTfulAPIServiceCallOptions] = None,
    ) -> Resource:
        """Calls test.httpgen.RESTfulAPIService.GetNestedResource."""
        opts = options or RESTfulAPIServiceCallOptions()
        content_type = opts.content_type or self._content_type
        if content_type != "application/json":
            raise NotImplementedError("only application/json is implemented")
        path = "/api/v1/orgs/{org_id}/teams/{team_id}/resources/{resource_id}"
        path = path.replace("{org_id}", urllib.parse.quote(str(req.org_id), safe=""))
        path = path.replace("{team_id}", urllib.parse.quote(str(req.team_id), safe=""))
        path = path.replace("{resource_id}", urllib.parse.quote(str(req.resource_id), safe=""))
        headers: dict[str, str] = dict(self._default_headers)
        headers["Content-Type"] = content_type
        headers["Accept"] = "application/json"
        if opts.headers:
            headers.update(opts.headers)
        if opts.api_key is not None:
            headers["X-API-Key"] = opts.api_key
        body: Optional[bytes] = None
        resp = self._request_with_retries(
            method="GET",
            url=self._base_url + path,
            headers=headers,
            body=body,
            timeout=opts.timeout if opts.timeout is not None else self._timeout,
        )
        if resp.status >= 400:
            self._raise_for_status(resp)
        if not resp.body:
            return Resource()
        return Resource.from_dict(json.loads(resp.body))

    def create_resource(
        self,
        req: CreateResourceRequest,
        options: Optional[RESTfulAPIServiceCallOptions] = None,
    ) -> Resource:
        """Calls test.httpgen.RESTfulAPIService.CreateResource."""
        opts = options or RESTfulAPIServiceCallOptions()
        content_type = opts.content_type or self._content_type
        if content_type != "application/json":
            raise NotImplementedError("only application/json is implemented")
        path = "/api/v1/resources"
        headers: dict[str, str] = dict(self._default_headers)
        headers["Content-Type"] = content_type
        headers["Accept"] = "application/json"
        if opts.headers:
            headers.update(opts.headers)
        if opts.api_key is not None:
            headers["X-API-Key"] = opts.api_key
        if opts.request_id is not None:
            headers["X-Request-ID"] = opts.request_id
        body = json.dumps(req.to_dict()).encode("utf-8")
        resp = self._request_with_retries(
            method="POST",
            url=self._base_url + path,
            headers=headers,
            body=body,
            timeout=opts.timeout if opts.timeout is not None else self._timeout,
        )
        if resp.status >= 400:
            self._raise_for_status(resp)
        if not resp.body:
            return Resource()
        return Resource.from_dict(json.loads(resp.body))

    def update_resource(
        self,
        req: UpdateResourceRequest,
        options: Optional[RESTfulAPIServiceCallOptions] = None,
    ) -> Resource:
        """Calls test.httpgen.RESTfulAPIService.UpdateResource."""
        opts = options or RESTfulAPIServiceCallOptions()
        content_type = opts.content_type or self._content_type
        if content_type != "application/json":
            raise NotImplementedError("only application/json is implemented")
        path = "/api/v1/resources/{resource_id}"
        path = path.replace("{resource_id}", urllib.parse.quote(str(req.resource_id), safe=""))
        headers: dict[str, str] = dict(self._default_headers)
        headers["Content-Type"] = content_type
        headers["Accept"] = "application/json"
        if opts.headers:
            headers.update(opts.headers)
        if opts.api_key is not None:
            headers["X-API-Key"] = opts.api_key
        body = json.dumps(req.to_dict()).encode("utf-8")
        resp = self._request_with_retries(
            method="PUT",
            url=self._base_url + path,
            headers=headers,
            body=body,
            timeout=opts.timeout if opts.timeout is not None else self._timeout,
        )
        if resp.status >= 400:
            self._raise_for_status(resp)
        if not resp.body:
            return Resource()
        return Resource.from_dict(json.loads(resp.body))

    def patch_resource(
        self,
        req: PatchResourceRequest,
        options: Optional[RESTfulAPIServiceCallOptions] = None,
    ) -> Resource:
        """Calls test.httpgen.RESTfulAPIService.PatchResource."""
        opts = options or RESTfulAPIServiceCallOptions()
        content_type = opts.content_type or self._content_type
        if content_type != "application/json":
            raise NotImplementedError("only application/json is implemented")
        path = "/api/v1/resources/{resource_id}"
        path = path.replace("{resource_id}", urllib.parse.quote(str(req.resource_id), safe=""))
        headers: dict[str, str] = dict(self._default_headers)
        headers["Content-Type"] = content_type
        headers["Accept"] = "application/json"
        if opts.headers:
            headers.update(opts.headers)
        if opts.api_key is not None:
            headers["X-API-Key"] = opts.api_key
        body = json.dumps(req.to_dict()).encode("utf-8")
        resp = self._request_with_retries(
            method="PATCH",
            url=self._base_url + path,
            headers=headers,
            body=body,
            timeout=opts.timeout if opts.timeout is not None else self._timeout,
        )
        if resp.status >= 400:
            self._raise_for_status(resp)
        if not resp.body:
            return Resource()
        return Resource.from_dict(json.loads(resp.body))

    def delete_resource(
        self,
        req: DeleteResourceRequest,
        options: Optional[RESTfulAPIServiceCallOptions] = None,
    ) -> DeleteResourceResponse:
        """Calls test.httpgen.RESTfulAPIService.DeleteResource."""
        opts = options or RESTfulAPIServiceCallOptions()
        content_type = opts.content_type or self._content_type
        if content_type != "application/json":
            raise NotImplementedError("only application/json is implemented")
        path = "/api/v1/resources/{resource_id}"
        path = path.replace("{resource_id}", urllib.parse.quote(str(req.resource_id), safe=""))
        headers: dict[str, str] = dict(self._default_headers)
        headers["Content-Type"] = content_type
        headers["Accept"] = "application/json"
        if opts.headers:
            headers.update(opts.headers)
        if opts.api_key is not None:
            headers["X-API-Key"] = opts.api_key
        body: Optional[bytes] = None
        resp = self._request_with_retries(
            method="DELETE",
            url=self._base_url + path,
            headers=headers,
            body=body,
            timeout=opts.timeout if opts.timeout is not None else self._timeout,
        )
        if resp.status >= 400:
            self._raise_for_status(resp)
        if not resp.body:
            return DeleteResourceResponse()
        return DeleteResourceResponse.from_dict(json.loads(resp.body))

    def default_post_method(
        self,
        req: DefaultPostRequest,
        options: Optional[RESTfulAPIServiceCallOptions] = None,
    ) -> DefaultPostResponse:
        """Calls test.httpgen.RESTfulAPIService.DefaultPostMethod."""
        opts = options or RESTfulAPIServiceCallOptions()
        content_type = opts.content_type or self._content_type
        if content_type != "application/json":
            raise NotImplementedError("only application/json is implemented")
        path = "/api/v1/legacy/action"
        headers: dict[str, str] = dict(self._default_headers)
        headers["Content-Type"] = content_type
        headers["Accept"] = "application/json"
        if opts.headers:
            headers.update(opts.headers)
        if opts.api_key is not None:
            headers["X-API-Key"] = opts.api_key
        body = json.dumps(req.to_dict()).encode("utf-8")
        resp = self._request_with_retries(
            method="POST",
            url=self._base_url + path,
            headers=headers,
            body=body,
            timeout=opts.timeout if opts.timeout is not None else self._timeout,
        )
        if resp.status >= 400:
            self._raise_for_status(resp)
        if not resp.body:
            return DefaultPostResponse()
        return DefaultPostResponse.from_dict(json.loads(resp.body))

    def search_resources(
        self,
        req: SearchResourcesRequest,
        options: Optional[RESTfulAPIServiceCallOptions] = None,
    ) -> ListResourcesResponse:
        """Calls test.httpgen.RESTfulAPIService.SearchResources."""
        opts = options or RESTfulAPIServiceCallOptions()
        content_type = opts.content_type or self._content_type
        if content_type != "application/json":
            raise NotImplementedError("only application/json is implemented")
        path = "/api/v1/resources/search"
        query_pairs: list[tuple[str, str]] = []
        if req.status_filter is not None and req.status_filter != 0:
            query_pairs.append(("status", str(req.status_filter)))
        if req.query:
            query_pairs.append(("q", str(req.query)))
        if query_pairs:
            path = path + "?" + urllib.parse.urlencode(query_pairs, doseq=True)
        headers: dict[str, str] = dict(self._default_headers)
        headers["Content-Type"] = content_type
        headers["Accept"] = "application/json"
        if opts.headers:
            headers.update(opts.headers)
        if opts.api_key is not None:
            headers["X-API-Key"] = opts.api_key
        body: Optional[bytes] = None
        resp = self._request_with_retries(
            method="GET",
            url=self._base_url + path,
            headers=headers,
            body=body,
            timeout=opts.timeout if opts.timeout is not None else self._timeout,
        )
        if resp.status >= 400:
            self._raise_for_status(resp)
        if not resp.body:
            return ListResourcesResponse()
        return ListResourcesResponse.from_dict(json.loads(resp.body))

    def _request_with_retries(
        self,
        method: str,
        url: str,
        headers: Mapping[str, str],
        body: Optional[bytes],
        timeout: Optional[float],
    ) -> HttpResponse:
        """Executes a request, retrying transport errors and 429/502/503/504."""
        attempts = max(1, self._max_retry_attempts)
        last_exc: Optional[Exception] = None
        for attempt in range(attempts):
            if attempt > 0:
                time.sleep(self._retry_backoff * (2 ** (attempt - 1)))
            try:
                resp = self._transport.request(
                    method=method, url=url, headers=headers, body=body, timeout=timeout,
                )
            except Exception as exc:  # noqa: BLE001 - transport errors vary by implementation
                last_exc = exc
                continue
            if attempt < attempts - 1 and resp.status in (429, 502, 503, 504):
                continue
            return resp
        assert last_exc is not None
        raise last_exc

    def _raise_for_status(self, resp: HttpResponse) -> None:
        """Map a non-2xx response to the most specific exception available."""
        body = resp.body or b""
        parsed: Any = None
        ctype = (resp.headers or {}).get("Content-Type", "")
        looks_jsonish = "json" in ctype.lower() or body[:1] in (b"{", b"[")
        if looks_jsonish:
            try:
                parsed = json.loads(body.decode("utf-8"))
            except (ValueError, UnicodeDecodeError):
                parsed = None
        if resp.status == 400 and isinstance(parsed, dict) and "violations" in parsed:
            violations = [
                FieldViolation(field=v.get("field", ""), description=v.get("description", ""))
                for v in parsed.get("violations", [])
            ]
            raise ValidationError(resp.status, body, resp.headers, violations)
        if isinstance(parsed, dict):
            for err_cls, required_keys in _ERROR_CLASSES:
                if required_keys and required_keys.issubset(parsed.keys()):
                    raise err_cls.populate(resp.status, body, resp.headers, parsed)
        raise ApiError(resp.status, body, resp.headers)

@dataclass
class BackwardCompatServiceClientOptions:
    """Construct-time options for BackwardCompatServiceClient."""
    transport: Optional[HttpTransport] = None
    default_headers: Optional[Mapping[str, str]] = None
    timeout: Optional[float] = None
    content_type: str = "application/json"
    # Total attempts including the first (1 = no retries). Transport errors and
    # HTTP 429/502/503/504 are retried with exponential backoff. SSE never retries.
    max_retry_attempts: int = 1
    # Delay in seconds before the first retry; doubles on each subsequent retry.
    retry_backoff: float = 0.25


@dataclass
class BackwardCompatServiceCallOptions:
    """Per-call options for BackwardCompatServiceClient methods."""
    headers: Optional[Mapping[str, str]] = None
    timeout: Optional[float] = None
    content_type: Optional[str] = None


class BackwardCompatServiceClient:
    """Generated client for test.httpgen.BackwardCompatService."""
    def __init__(
        self,
        base_url: str,
        options: Optional[BackwardCompatServiceClientOptions] = None,
    ) -> None:
        self._base_url = base_url.rstrip("/")
        opts = options or BackwardCompatServiceClientOptions()
        self._transport: HttpTransport = opts.transport or UrllibTransport()
        self._default_headers: dict[str, str] = dict(opts.default_headers or {})
        self._timeout = opts.timeout
        self._content_type = opts.content_type
        self._max_retry_attempts = opts.max_retry_attempts
        self._retry_backoff = opts.retry_backoff

    def legacy_action(
        self,
        req: LegacyRequest,
        options: Optional[BackwardCompatServiceCallOptions] = None,
    ) -> LegacyResponse:
        """Calls test.httpgen.BackwardCompatService.LegacyAction."""
        opts = options or BackwardCompatServiceCallOptions()
        content_type = opts.content_type or self._content_type
        if content_type != "application/json":
            raise NotImplementedError("only application/json is implemented")
        path = "/legacyAction"
        headers: dict[str, str] = dict(self._default_headers)
        headers["Content-Type"] = content_type
        headers["Accept"] = "application/json"
        if opts.headers:
            headers.update(opts.headers)
        body = json.dumps(req.to_dict()).encode("utf-8")
        resp = self._request_with_retries(
            method="POST",
            url=self._base_url + path,
            headers=headers,
            body=body,
            timeout=opts.timeout if opts.timeout is not None else self._timeout,
        )
        if resp.status >= 400:
            self._raise_for_status(resp)
        if not resp.body:
            return LegacyResponse()
        return LegacyResponse.from_dict(json.loads(resp.body))

    def _request_with_retries(
        self,
        method: str,
        url: str,
        headers: Mapping[str, str],
        body: Optional[bytes],
        timeout: Optional[float],
    ) -> HttpResponse:
        """Executes a request, retrying transport errors and 429/502/503/504."""
        attempts = max(1, self._max_retry_attempts)
        last_exc: Optional[Exception] = None
        for attempt in range(attempts):
            if attempt > 0:
                time.sleep(self._retry_backoff * (2 ** (attempt - 1)))
            try:
                resp = self._transport.request(
                    method=method, url=url, headers=headers, body=body, timeout=timeout,
                )
            except Exception as exc:  # noqa: BLE001 - transport errors vary by implementation
                last_exc = exc
                continue
            if attempt < attempts - 1 and resp.status in (429, 502, 503, 504):
                continue
            return resp
        assert last_exc is not None
        raise last_exc

    def _raise_for_status(self, resp: HttpResponse) -> None:
        """Map a non-2xx response to the most specific exception available."""
        body = resp.body or b""
        parsed: Any = None
        ctype = (resp.headers or {}).get("Content-Type", "")
        looks_jsonish = "json" in ctype.lower() or body[:1] in (b"{", b"[")
        if looks_jsonish:
            try:
                parsed = json.loads(body.decode("utf-8"))
            except (ValueError, UnicodeDecodeError):
                parsed = None
        if resp.status == 400 and isinstance(parsed, dict) and "violations" in parsed:
            violations = [
                FieldViolation(field=v.get("field", ""), description=v.get("description", ""))
                for v in parsed.get("violations", [])
            ]
            raise ValidationError(resp.status, body, resp.headers, violations)
        if isinstance(parsed, dict):
            for err_cls, required_keys in _ERROR_CLASSES:
                if required_keys and required_keys.issubset(parsed.keys()):
                    raise err_cls.populate(resp.status, body, resp.headers, parsed)
        raise ApiError(resp.status, body, resp.headers)

