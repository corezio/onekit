syntax = "proto3";
package invalid_path_param;
option go_package = "github.com/1homsi/onekit/internal/tsservergen/testdata/generated;generated";

import "onekit/http/annotations.proto";

// Request is missing the 'id' field that the path parameter references.
message GetItemRequest {
  string name = 1;
}

message Item {
  string id = 1;
  string name = 2;
}

service ItemService {
  option (onekit.http.service_config) = {
    base_path: "/api/v1"
  };

  rpc GetItem(GetItemRequest) returns (Item) {
    option (onekit.http.config) = {
      path: "/items/{id}"
      method: HTTP_METHOD_GET
    };
  }
}
